# CodexNeo Integration Handover

## Current State

- 2026-07-13: Completed CN-069 on isolated branch `codexneo-unified-pool`. Live comparison showed the main Accounts API had current weekly values for all six accounts while CodexNeo showed those same values under 5h and either blank or stale values under Weekly. The latest stored records for all six had `window_minutes=10080` but were persisted in the legacy `primary` slot; four accounts also retained older weekly-duration `secondary` rows. The main Accounts mapper already normalizes by duration, but CodexNeo merged slot names directly. CodexNeo now normalizes registry and history windows before freshness/merge decisions, promotes weekly-duration primary data to Weekly, prefers the current weekly candidate over stale secondary history, preserves genuine 5h+weekly pairs, and leaves 5h blank when the provider supplies no short window.
- CN-069 evidence/runtime note: three focused regressions failed before the corrections and passed afterward; the focused account suite passed (`19`), all CodexNeo unit/integration tests passed (`130`, with the existing post-run `aiosqlite` event-loop warning), scoped Ruff and `ty` passed, the strict change and all `31` stable OpenSpecs passed. `accounts.py` was mirrored into the active portable backend with a matching SHA-256 (`2BEEC3EC3E21289A06EC5E7FACC0630D6011CF47ED4BCB7FD6E68AD8699E1721`), and both bundled Python runtimes passed `py_compile`. The portable app/backend and Codex Desktop were not restarted, so activation occurs on the user's next manual Codex LB restart.

- 2026-07-12: Completed CN-068 on isolated branch `codexneo-unified-pool`. The live defect had three related causes: managed Backup registry rows retained the mutable root `auth_path`, so a later CodexGO rotation could make an older snapshot resolve as the new live identity; location bulk actions counted four physical root/backup rows instead of the three logical table accounts; and usage queries included only filesystem-matched account ids, excluding pool-only Accounts rows even when usage history existed. Backup registry rows now persist stable metadata only, identity resolution prefers immutable account-specific snapshots, bulk actions/states deduplicate logical identities, and quota lookup covers the full canonical Accounts pool.
- CN-068 evidence/runtime note: three focused regressions failed before the correction and passed after it. Focused location/account tests passed (`27`), all CodexNeo unit/integration tests passed (`127`), scoped Ruff and `ty` passed, the strict OpenSpec change and all `31` stable specs passed, and diff validation passed. `accounts.py` and `locations.py` were mirrored into the active portable backend with `2/2` SHA-256 matches; both bundled Python runtimes passed `py_compile`. The portable Electron app/backend and Codex Desktop were not restarted, so the running process still has its previously loaded modules and the fix activates on the user's next manual Codex LB restart.

- 2026-07-12: Completed CN-066 on isolated branch `codexneo-unified-pool`. CodexGO root-file delivery and CodexGO API delivery now ingest into the same canonical Codex LB Accounts pool and app-owned snapshot store. Provider replacement no longer retires the previous identity, same-identity delivery refreshes its managed snapshot, and a backend reconciler detects valid root `auth.json` changes even while the CodexNeo page is closed. CodexNeo account reads begin with canonical Accounts rows and add live/backup/provider state, so the CodexNeo and Codex LB tables no longer represent separate inventories.
- CN-066 UI and safety behavior: CodexNeo now has separate Accounts and Activity tabs. Accounts embeds Auth->API and CodexGO, removes the OpenAI/Management log toggles, separates import-source and export-destination folders, exposes one Export selected action, confirmation-gated Delete selected, and row Switch / Switch & Restart. Delete removes selected managed credentials from the pool, Backup, and app-managed Codex Home but preserves de-associated usage history. Automatic quota deletion requires a successful latest weekly reading of exactly 0% remaining; positive and unknown values are retained. Activity combines safe Codex LB, CodexNeo, and provider events, supports filters, scrolls internally, and is pruned after exactly 24 hours by the backend even if the page is closed.
- CN-066 verification/runtime note: all `127` CodexNeo backend tests and `41` CodexNeo frontend tests passed. Ruff, ESLint, TypeScript/Vite production build, strict OpenSpec validation, and mocked rendered Accounts/Activity visual smoke passed; the only build warning was the pre-existing large-chunk notice. Eleven Python files and all `34` compiled static files were mirrored to both active portable roots with `22` Python and `68` static SHA-256 matches. Both portable Python runtimes passed `py_compile`, and live `/health` remained `ok`. The portable Electron app, its already-running backend, and Codex Desktop were not restarted; staged Python behavior activates on the next normal portable restart.

- 2026-07-12: Completed CN-067 follow-up after the live page still showed a false account mismatch and the separate Activity page left a fixed-height blank region. Root cause: the health card compared a legacy locally discovered snapshot subset with the canonical encrypted Accounts pool; CodexGo is expected to rotate the single live `auth.json`, so those inventories are independent. `health.py` now reports the canonical managed-pool count as `Shared account pool` and leaves Codex Home/backup counts as separate diagnostics. The Activity page now has a viewport-bound flex layout, with filter controls and retention notice visible while only the event stream scrolls.
- CN-067 verification/runtime note: RED/GREEN health regression passed (`5` tests); focused Activity/Page Vitest passed (`34` tests); expanded CodexNeo backend tests passed (`124` tests, with one pre-existing `aiosqlite` post-run event-loop warning); Ruff, ESLint, TypeScript, production build, and strict OpenSpec validation passed. Source/static files were mirrored into `dist\CodexIB-Electron-Portable`; the health module SHA-256 matched both portable Python roots, and the final static bundle is `index-DxfbhHJO.js`. Restarted only the portable Electron app (final Electron PID `47204`, backend PID `26964`); `/health` returned `ok`, `/api/codexneo/health` returned `Shared account pool`, `ok`, and `12 managed account(s)`. A 1568x1003 production-page smoke measured the Activity region at y=216..915, the retention strip at y=881..914, and the 5,648px event stream scrolling inside a 523px log region with zero console errors. Codex Desktop was not restarted.
- History retention is complete and verified in source: raw request logs plus primary/additional usage samples expire after 24 hours, while compact request aggregates preserve Dashboard, Reports, API-key, account, model, token, cost, and error statistics. The portable runtime mirror remains pending until the consolidated portable branch is assembled; no app restart is authorized for this consolidation.
- Two obsolete pre-migration database copies were removed from `portable-data`, freeing 748.8 MB. Do not treat the live store as already pruned until a post-restart check confirms the new migration and retention pass.

- Workspace: `H:\Opencode IDE\codex-ib\codex-lb`
- Active development branch: `codexneo-portable-windows`
- Upstream/original branch: `main`
- Target app: Codex IB / codex-lb with the CodexNeo web tab and the Windows Electron portable package.
- Active portable package: `dist\CodexIB-Electron-Portable`
- Portable executable: `dist\CodexIB-Electron-Portable\Codex IB.exe`
- Portable runtime data: `dist\CodexIB-Electron-Portable\portable-data`
- Portable live DB: `dist\CodexIB-Electron-Portable\portable-data\store.db`
- Real Codex Home target: `C:\Users\rcgok\.codex` unless the user changes Codex Home in the UI.
- Reference-only Windows app/repo: `H:\Opencode IDE\RC Codex Auth Switcher`; use it for parity evidence, not as the implementation target.

## Branch Policy

- `codexneo-portable-windows` is the main working branch for all portable Windows Codex IB / CodexNeo development.
- `main` should remain the original fork/upstream branch. Keep it clean for upstream updates, then port/cherry-pick needed changes into `codexneo-portable-windows`.
- The repo should have only two branches locally/remotely unless the user explicitly asks otherwise: `main` and `codexneo-portable-windows`.
- Current verified branch shape on 2026-06-27:
  - `main` at `17c1762` / `origin/main`
  - `codexneo-portable-windows` at `0cc580f` / `origin/codexneo-portable-windows`
- Do not delete branches, package folders, data folders, or startup entries without verifying the target path and confirming the item is not part of the active portable app.

## Portable Runtime Rules

- The portable app does not necessarily run source-tree Python files. It launches bundled Python from `dist\CodexIB-Electron-Portable\.python\python.exe` and app code copied under `dist\CodexIB-Electron-Portable\app`.
- When a fix must affect the current portable app after the user's next restart, patch both:
  - source tree files under `app/`, `frontend/`, `desktop/`, `scripts/`, or tests as appropriate
  - mirrored runtime files under `dist\CodexIB-Electron-Portable\app\...` when the bundled portable copy needs the fix immediately
- Do not restart `Codex IB.exe` / the portable Electron app unless the user explicitly says to. The user has repeatedly preferred to restart it manually.
- Do not restart Codex Desktop automatically. Auth->API Set/Revert should write and verify config only and return a manual-restart message. Explicit `Restart Codex` and `Switch & Restart` actions are the only intended runtime restart actions.
- If a running app still shows old behavior after code is patched, first determine whether the source server or portable bundled server is running on `127.0.0.1:2455`.

## Current Behavior Summary

- CodexNeo tab contains Auth->API controls, CodexGO auth controls, health diagnostics, Codex Home controls, Codex Home account table, import/export, auto refresh, auto sync, minimize-to-tray controls, and activity log.
- CodexNeo account discovery reads configured Codex Home, including root `auth.json`, managed account snapshots, encoded snapshot filenames, and app-owned Backup snapshots.
- `C:\Users\rcgok\.codex\auth.json` is intentionally treated as a real active/root Codex account source and is synced into CodexNeo/Codex IB when valid.
- Main Accounts and CodexNeo are reconciled by explicit/auto sync, not by read-only account listing. Read routes should not mutate data.
- Generated duplicate `__copy` Accounts rows may be hidden or consolidated only on approved write/sync paths; do not delete arbitrary account rows on a read.
- Account pause/status changes made by the operator in the Accounts tab must persist across restart and auth sync.
- Account delete now preserves request/usage history by default. Only explicit `delete_history=true` hard-deletes history.
- Old non-portable `C:\Users\rcgok\.codex-lb` data was removed during cleanup. Active portable data is under `dist\CodexIB-Electron-Portable\portable-data`.

## Design Notes

- OpenSpec change folder: `openspec/changes/add-codexneo-windows-integration`
- Local settings should be stored under codex-lb's configured data directory, with buyer token encrypted through the existing `TokenEncryptor`.
- Host writes must back up and atomically replace `%USERPROFILE%\.codex\config.toml` and `%USERPROFILE%\.codex\auth.json`.
- Buyer tokens must not be logged, returned from GET responses, or written to plaintext app settings.
- Runtime restart policy: restart the native codex-ib/codex-lb source server only when needed to load or verify code changes and not prohibited by the user. Do not restart the portable Electron app or Codex Desktop without explicit user permission. Auth->API Set/Revert must not auto-restart Codex Desktop; they should instruct the user to restart manually.

## Latest 2026-06-27 Fixes

- Fixed root-auth mismatch: CodexNeo now includes valid top-level `C:\Users\rcgok\.codex\auth.json` and avoids double-counting it when it matches a managed registry/snapshot identity.
- Fixed Auth->API Set/Revert restart behavior: Set/Revert no longer stop or restart Codex Desktop. This prevents very new or in-flight Codex chats from being lost when switching between CodexNeo Windows app and Codex IB through the auth API config path.
- Fixed Accounts paused-state persistence: auth sync/import must preserve an operator-paused account instead of overwriting it back to active.
- Fixed CodexNeo/Codex IB account mismatch root causes:
  - old hidden non-portable server/startup artifacts were removed
  - portable runtime was confirmed as the only intended app server
  - CodexNeo visible account count now merges root `auth.json` with matching registered snapshots by parsed auth identity
  - explicit Sync remains the write route for reconciliation
- Fixed delete-history behavior:
  - `request_logs` were already preserved by default
  - `usage_history` and `additional_usage_history` previously had `ON DELETE CASCADE` and were being deleted
  - new migration `app\db\alembic\versions\20260627_000000_preserve_usage_history_on_account_delete.py` changes both usage tables to nullable `account_id` with `ON DELETE SET NULL`
  - `AccountsRepository.delete(..., delete_history=False)` now detaches usage rows instead of deleting them
  - `delete_history=True` still hard-deletes request logs and usage rows
  - same model/repository/migration code was mirrored into `dist\CodexIB-Electron-Portable\app\...`
- Patched the live portable database in place:
  - backup created at `dist\CodexIB-Electron-Portable\portable-data\store.db.bak-20260627-053740`
  - `usage_history` and `additional_usage_history` foreign keys now use `ON DELETE SET NULL`
  - `PRAGMA integrity_check` returned `ok`
  - `PRAGMA foreign_key_check` returned 0 issues
- Imported rough CodexNeo Windows aggregate usage into the portable DB as synthetic request-log rows:
  - source marker: `codexneo-windows-estimate`
  - model marker: `gpt-5.5-codex-estimate`
  - target source values came from the CodexNeo Windows aggregate screenshot plus `H:\Opencode IDE\RC Codex Auth Switcher\dist\rc-codex-auth-data\api-routing.json`
  - intended rough targets: today `199,533,710` tokens, lifetime about `2.811B` tokens, cost about `$9,380.18`
  - rows are idempotent by source marker; rerun should delete and replace prior `codexneo-windows-estimate` rows
- Note: while the portable app is running, verification/API requests may add real logs and make totals drift slightly above the seeded target. Recalculate the synthetic rows against the current base totals if exact rough display alignment is needed again.

## Latest 2026-07-03 Fixes

- Commit `be6f334 fix(codexneo): reconcile usage plan display` added guarded usage-plan reconciliation and CodexNeo table parity for reconciled Codex IB plan/status. `CODEX_LB_USAGE_REFRESH_ALLOW_PLAN_RECONCILE` defaults to `false` in source, while the local portable runtime `.env` was created with `CODEX_LB_USAGE_REFRESH_ALLOW_PLAN_RECONCILE=true` for the user's requested portable behavior. Workspace identity conflicts remain blocked.
- The same change fixed the CodexNeo account table mismatch after upstream plan drift: CodexNeo now overlays matched Codex IB `plan_type`, fetches monthly usage when needed, and applies the effective quota-status rule so exhausted free accounts show `plan=free`, `codexIbStatus=quota_exceeded`, and `routable=false` instead of stale `pro` / `Ready`.
- Commit `7cc90c8 fix(codexneo): log codexgo auto refresh` made background CodexGO auto-refresh visible in the Management activity log. The scheduler now logs token-free skipped, started, applied, and failed entries through `CodexNeoActivityLogService` and respects the existing Management log stream setting.
- Runtime note: source changes from these commits were mirrored into the active portable runtime where required, and only the portable backend process on `127.0.0.1:2455` was restarted for verification. The Electron shell and Codex Desktop were not restarted.
- Verification evidence from the implementation turns: usage updater and CodexNeo account tests passed, scheduler tests passed, Ruff passed for touched files, `openspec validate allow-usage-plan-reconcile --strict`, `openspec validate add-codexneo-windows-integration --strict`, and `openspec validate --specs` passed. Live checks confirmed `/health` returned `ok`, `/api/codexneo/accounts` returned the corrected free/quota-exceeded rows, and the Management log included `Management task CodexGO auto-refresh -> started.` plus `-> applied; ...ms.` after a forced scheduler pass.

## Latest 2026-07-09 Fixes

- Added a second CodexNeo auto-delete toggle, `Auto delete quota exceeded`, persisted independently from `Auto delete free/auth required`.
- The new backend write action deletes only matched CodexNeo/Codex IB accounts with status `quota_exceeded`, latest weekly remaining usage at `2%` or lower, and no matching Backup snapshot.
- The 5-hour remaining usage value is ignored for this rule: weekly-above-threshold accounts are kept even when 5-hour remaining usage is `0%`.
- Backed-up accounts are explicitly skipped so safe copies are not removed.
- Verification passed for focused/backend frontend tests, Ruff, TypeScript, production frontend build, OpenSpec strict/spec validation, portable runtime mirror/hash checks, and portable backend `py_compile`.
- Per user instruction, the portable app and Codex Desktop were not restarted during implementation or verification.
- Follow-up debug found the live endpoint was running but returned `Auto delete found 0 eligible quota-exceeded account(s)` because the highlighted rows were stored as `accounts.status=active` while the CodexNeo table displayed derived `Quota exceeded` from weekly exhaustion. The cleanup rule now also accepts stored-active rows whose shared quota calculation derives `quota_exceeded`, while still skipping rate-limited/paused/other non-active statuses and backed-up accounts. The fixed `sync.py` was mirrored into the portable runtime, but the running portable backend still requires a restart before this loaded-module fix can affect live deletion.

## Latest 2026-07-10 Provider Compatibility Fixes

- CodexNeo's native Codex provider default is `http://127.0.0.1:2455/backend-api/codex`; generic OpenAI-compatible `/v1` clients remain unchanged. The provider test now requires a direct 2xx JSON model catalog with a top-level `models` list, so generic `object`/`data` envelopes and redirects fail safely.
- The exact legacy local `/v1` value migrates atomically to the native endpoint. The migration now copies the original settings JSON and replaces only `codex_api_base_url`, preserving unknown future settings instead of rebuilding a whitelist.
- The pre-migration portable settings backup and current settings have matching top-level key sets (15 each); no live unknown setting was lost during the earlier migration. The active setting is the native endpoint.
- TDD evidence: both new regressions failed before the correction (unknown-key loss and HTTP 302 acceptance) and passed afterward. Current scoped verification: `102 passed` CodexNeo unit/integration tests, `38 passed` CodexNeo frontend tests, Ruff, frontend TypeScript, `openspec validate fix-codexneo-native-api-provider --strict`, and baseline OpenSpec validation all pass.
- Portable runtime synchronization and its user-authorized restart are intentionally owned by the concurrent Responses Lite task. It will copy the current `app/modules/codexneo/service.py` and `health.py` into both portable runtime copies together with the Responses Lite repair; this task does not package or restart the app.

## Latest 2026-07-10 Responses Lite compatibility fix

- Root cause for GPT-5.6 Sol toolless turns was confirmed in `app\core\openai\requests.py`: `input.type = "additional_tools"` is a developer-role protocol object, and the old instruction normalizer dropped it. Review of PR #1158 plus the current official Codex client tests showed that preserving only this item was incomplete: Responses Lite also deliberately keeps its base developer instruction as the next message inside `input`, with no instruction lift.
- The final native repair preserves the complete ordered Lite input envelope whenever `additional_tools` is present. Ordinary non-Lite system/developer normalization remains unchanged, and the proxy never converts the embedded bundle into top-level tools.
- Transport behavior now follows Codex's native split. Direct/fallback HTTP and compact requests emit `x-openai-internal-codex-responses-lite: true`. HTTP-bridge and native WebSocket requests carry `ws_request_header_x_openai_internal_codex_responses_lite: "true"` in `response.create.client_metadata`; upstream WebSocket handshakes omit the HTTP-only header. Only native Codex identity headers can opt into this internal transport contract.
- No model-catalog workaround was applied. The restarted live catalog reports `gpt-5.6-sol` with `use_responses_lite=true`, `tool_mode=code_mode_only`, `multi_agent_version=v2`, `shell_type=shell_command`, and `apply_patch_tool_type=freeform`.
- Regression evidence: the focused RED run failed at all expected seams, then the corrected focused suite passed `13` tests. The complete affected suite passed `751` tests in 5m29s; Ruff, `git diff --check`, strict change validation, and all `30` OpenSpec specs passed. The only suite warnings were the existing Starlette/httpx deprecation and an unrelated aiosqlite event-loop cleanup warning in a previous-response WebSocket test.
- Portable deployment: synchronized the six Responses Lite production modules plus coordinated CodexNeo `service.py` and `health.py` into both `dist\CodexIB-Electron-Portable\app` and bundled `.venv\Lib\site-packages`. All eight source/runtime/site-packages SHA-256 values match, portable `py_compile` passed, and an isolated portable import proved exact input/header/WebSocket-metadata behavior.
- The user closed the old app and the portable build was relaunched through the background GUI driver. Electron PID `92880` owns backend PID `7032` on `127.0.0.1:2455`. `/health`, `/health/ready`, and CodexNeo health all return `ok`; the bridge ring includes `RC-PC`; `/internal/drain/status` reports `in_flight=0`; GPT-5.6 metadata is present. The earlier handcrafted raw-stream observation no longer leaves residual bridge work after this clean restart.
- A prior ephemeral read-only Sol smoke executed `Get-Location`, but its isolated CLI configuration used fallback model metadata. Exact Lite wire behavior is therefore proven by the source/portable transport regressions and portable import smoke, while the live checks prove the corrected build and native Lite model metadata are loaded.

## Latest 2026-07-11 Proxy Recovery and CodexGO Retention

- Completed CN-063, the stuck HTTP-bridge `response.create` gate retirement and cancellation-safe reconnect/resource-handoff repair. A session generation is eligible only after its gate has been held for the configurable 300-second threshold with no upstream event; eligibility is revalidated under lifecycle and pending locks. Retirement permanently tombstones and expected-generation-detaches that generation, then settles reader/sends, pending work, event queues, response-create/admission gates, reservations, durable ownership, upstream socket, and account lease. Reconnect is serialized, provisional resources remain owned under cancellation, and displaced installed resources transfer to exactly one tracked settlement task. Normal reader-originated close/reconnect remains unchanged.
- CN-063 required six commits as independent review exposed progressively narrower cancellation, initial-send, terminal-settlement, reconnect-serialization, and atomic-handoff races: `c068903`, `73786f8`, `4e4180f`, `a70ecc5`, `4c033f1`, and `07b1239`. Final controller verification passed the complete `225`-test HTTP-bridge unit file and `6` selected timeout/reconnect integrations; Ruff, scoped `ty`, strict OpenSpec, and `git diff --check` passed. Fresh exact-delta and whole-range reviews reported no Critical, Important, or Minor findings.
- Completed CN-064 for affected thread `019f4868-4256-77d3-9229-2acf4953c4da`. Root cause of the two visible encrypted-content verification errors was local compaction normalization: Codex-LB preserved upstream `encrypted_content` but dropped its canonical non-empty compaction item `id`; Codex then synthesized a different `cmp_*` id, and upstream rejected continuation replay because the encrypted payload was bound to the original item id. Commit `36fb507` now preserves the exact canonical id in both emitted SSE locations while keeping the legacy top-level summary idless. Four focused contract/integration tests, Ruff, strict OpenSpec, diff scope, and independent review passed.
- The already-poisoned CN-064 thread cannot be retroactively repaired: its persisted encrypted continuation already contains the mismatched pair. Retry from a clean turn/thread. A secret-safe live smoke on the loaded repair returned 200/completed for an ordinary request, compacted with the exact upstream `cmp_*` id, then replayed that exact compaction item and returned 200/completed with `REPLAY_OK`. A hidden-error query returned zero; the smoke session rows themselves were absent, so this is transport/event evidence and not evidence of three persisted request-log rows.
- The completed proxy changes are archived at `openspec/changes/archive/2026-07-11-retire-stuck-http-bridge-response-create-gates` and `openspec/changes/archive/2026-07-11-bridge-codex-compaction-trigger`. Their stable deltas are in `openspec/specs/proxy-admission-control/spec.md` and `openspec/specs/responses-api-compat/spec.md`; strict validation passed all `31` stable specs.
- Portable proxy deployment mirrored eight affected source modules into both `dist\CodexIB-Electron-Portable\app` and `dist\CodexIB-Electron-Portable\.venv\Lib\site-packages\app`. All `16/16` source-to-target SHA-256 comparisons matched; both portable roots passed `py_compile` and root-pinned import checks.
- Completed CN-065 after the user reported deleting about `1330` stale accounts and an ongoing CodexGO `auth.json` backup storm. The root cause had two coupled parts: every CodexGO use/refresh created another timestamped `auth.json.codexgo-backup-*` recovery file without pruning, and each transient root identity could be imported into encrypted Accounts and then made durable by reverse sync. Retention now applies only to the exact CodexGO-managed family, protects the current recovery copy, caps the family at `10`, protects at least `3` before age pruning, removes additional files only after more than 14 exact days, and leaves unrelated backup families untouched.
- CN-065 stale-account reconciliation derives a stable normalized identity before any root/backup/state mutation, stores only token-free SHA-256 provenance, queues only a previously tracked CodexGO root identity when a different identity replaces it, excludes the active transient identity from reverse sync, and removes a retired Accounts row only when no matching non-root managed Codex Home or app-owned Backup source exists. Matching managed sources are preserved, historical request/usage/audit data remains detached rather than cascaded, failed retirement remains queued for retry, and root replacement/reconciliation share one process-local guard. The production scheduler now constructs its service with account reconciliation enabled.
- CN-065 verification passed the broad related suite (`111 passed in 41.11s`) and the independent focused review gate (`53 passed`). Scoped Ruff, scoped `ty`, strict `bound-codexgo-auth-backup-retention` validation, all `31/31` stable OpenSpecs, and `git diff --check` passed. `service.py`, `sync.py`, and `scheduler.py` were mirrored into both portable Python roots with `6/6` SHA-256 matches, and both portable runtimes passed `py_compile`. After the natural-reload smoke completed task 5.4, the stable CodexNeo Windows integration requirements were synchronized and the change was archived at `openspec/changes/archive/2026-07-11-bound-codexgo-auth-backup-retention`.
- Activation completed on 2026-07-12 without an agent-initiated process action: the next natural Electron-managed reload started Electron PID `67204` and portable backend PID `51836` at `01:24` on `127.0.0.1:2455`. A read-only check found `/health` and `/health/ready` `ok`, drain and bridge drain `false`, and zero in-flight requests. The scheduled `01:39` CodexGO refresh applied successfully; the managed backup family remained capped at `10`, and the newest protected backup plus root auth write were both created at `01:39:40`. Token-free reconciliation state existed at schema `1` with an active fingerprint present and zero retired fingerprints. Accounts and managed snapshots were aligned at `29/29` (`28` active and `1` reauth-required), while the newest snapshot still predated the refresh at `01:26`, so the live refresh did not promote another transient root snapshot. No cross-identity A-to-B rotation occurred, so the tested retired-account deletion path had no live identity to remove.
- Completed CN-066, the focused Codex parity smoke runner. The semantic `codex_parity_smoke` marker owns 16 approved functions that currently collect as 18 cases across Responses Lite, compact, HTTP bridge, native WebSocket, and negotiated model-catalog contracts. Membership stays with each test rather than a duplicated node-id manifest. The Make contract requires a `.PHONY` declaration, no target prerequisites, and exactly the approved two-command recipe. The focused lane remains an early-feedback aid only: full affected suites, static/OpenSpec checks, CI, and release gates remain authoritative.
- CN-066 execution evidence: GNU Make is unavailable on this Windows host, so it was not installed or invoked. Final review and its complete re-review found prerequisite-declaration bypasses in the structural assertion; the shared contract guard now covers direct, duplicate, and multi-target prerequisite declarations without changing the Makefile, and its workflow file passed `2` tests. The direct Windows recipe passed `18` cases with `3973` deselected and the existing Starlette warning. The explicit 16-function inventory previously passed the same `18` cases; mechanical marker/direct comparison found a zero case-id set delta.
- CN-066 completion evidence: the final group passed `617` unit/contract tests, including the workflow contract; the other complete affected groups passed `87` compact/Responses/catalog tests and `132` HTTP-bridge/native-WebSocket tests. The one-line stale settings-fixture correction was independently reviewed and retained. Ruff lint passed across all changed tests; the workflow file passed Ruff format; the four legacy format proposals were proven pre-existing and base-identical at `e88fbf3`; strict active/stable OpenSpec validation and diff checks passed; independent Task 1 and Task 2A reviews were recorded.
- CN-066 final hardening commit is `0ab823c`. A fresh independent re-review inspected the complete target-list parser, multi-target regression, three-occurrence invariant, tracking counts, and three-file hardening scope; it reported no Critical, Important, or Minor findings and marked the branch ready to merge.
- CN-066 changed only test metadata/workflow and documentation. No production source, portable package, database, API, runtime, process restart, install, or remote state changed.

## Progress Log

- 2026-06-22: Recreated missing root handover and OpenSpec artifacts in the live checkout before implementation.
- 2026-06-22: Added backend RED tests for CodexNeo settings, provider config, CodexGO auth replacement, validation, and scheduler behavior.
- 2026-06-22: Implemented CodexNeo backend schemas, service, API router, and CodexGO refresh scheduler.
- 2026-06-22: Wired the CodexNeo API router and CodexGO refresh scheduler into `app/main.py`.
- 2026-06-22: Added frontend RED tests for CodexNeo nav and page controls.
- 2026-06-22: Implemented CodexNeo frontend schemas, API client, React Query hook, page component, route, and header nav item.
- 2026-06-22: Fixed Ruff cleanup in the new backend test file after verification caught unused imports.
- 2026-06-22: Browser-smoked the CodexNeo page on a temporary updated server at `http://127.0.0.1:2460/codexneo`, then stopped the server and removed temporary logs.
- 2026-06-22: Installed `@fission-ai/openspec@1.4.1` globally via npm to provide the `openspec` CLI.
- 2026-06-22: Restarted the native `127.0.0.1:2455` codex-lb server from `.venv\Scripts\codex-lb.exe` so the live process loads CodexNeo.
- 2026-06-22: Added `CODEXNEO_IMPLEMENTATION_LIST.md` as the root intake list for future CodexNeo feature requests before implementation starts.
- 2026-06-22: Added CN-002 Activity log and CN-003 Codex Home account discovery intake items after inspecting the RC Codex Auth Switcher reference implementation.
- 2026-06-22: Clarified CN-002 through CN-007 from the screenshot: activity log includes both streams, account table lives in CodexNeo first, Auth->API Set/Revert must restart Codex, Codex home controls are in scope, import/export controls are in scope, and row actions are a later safe slice.
- 2026-06-22: Started implementation pass for CN-002 and CN-003 by updating OpenSpec requirements and tasks before code.
- 2026-06-22: Validated updated OpenSpec change after adding CN-002/CN-003 requirements.
- 2026-06-22: Added backend RED tests for CN-002 activity log sanitization/clear behavior, log toggle settings persistence, and CN-003 registry-based account discovery.
- 2026-06-22: Implemented CN-002 backend activity-log service/API/middleware and CN-003 read-only Codex registry account discovery API.
- 2026-06-22: Added frontend RED expectations for CN-002 activity log toggles/panel/clear action and CN-003 Codex Home accounts table.
- 2026-06-22: Implemented frontend schemas/API/hook/page support for CN-002 activity log controls/panel and CN-003 account table.
- 2026-06-22: Fixed Ruff import-order issue in the CodexNeo API module found during verification.
- 2026-06-22: Applied Ruff's single-file import sorter to normalize the CodexNeo API schema import order.
- 2026-06-22: Marked OpenSpec CN-002/CN-003 tasks complete and updated the implementation list statuses to Verified for CN-002 and CN-003.
- 2026-06-22: Started CN-004 implementation pass by adding OpenSpec scenarios/tasks for restart-after Auth->API Set/Revert and restart-failure reporting.
- 2026-06-22: Added CN-004 backend RED tests for restart-after set/revert, restart failure reporting, activity-log recording, and no-restart on config write failure.
- 2026-06-22: Implemented CN-004 backend restart abstraction, restart response fields, config verification gates, and Auth->API Set/Revert restart wiring.
- 2026-06-22: Fixed misplaced CodexGO auth validation lines found by CN-004 test failure so provider revert verification no longer references out-of-scope token data.
- 2026-06-22: Patched existing CodexNeo service Set/Revert tests to inject a fake restart provider so verification does not stop or relaunch the real Codex app.
- 2026-06-22: Completed CN-004 verification without invoking live Auth->API Set/Revert endpoints or stopping Codex during testing.
- 2026-06-22: Marked CN-004 OpenSpec tasks complete and implementation-list status Verified.
- 2026-06-22: Started CN-005/CN-006/CN-007 combined pass with accepted safeguards: backend folder picker plus manual path fallback, timestamped/no-overwrite exports, command-backed imports/switch/delete, metadata/API routing local stores, and no live Codex restart/account mutation during verification.
- 2026-06-22: Added backend RED tests for CN-005 Codex Home save/reset/picker fallback, CN-006 import/export command routing and timestamped selected exports, and CN-007 metadata/API/switch/delete actions using fake command/restart providers.
- 2026-06-22: Implemented backend CN-005/CN-006/CN-007 services/routes and verified the new backend tests pass: `tests/unit/test_codexneo_home_ops.py tests/unit/test_codexneo_import_export_actions.py` 5 passed.
- 2026-06-22: Finished the first CN-005/CN-006/CN-007 frontend wiring pass for Codex Home controls, import/export controls, and confirmation-gated selected-account actions; fixed a Windows placeholder string before running TypeScript checks.
- 2026-06-22: Cleaned CN-005/CN-006/CN-007 backend formatting issues caught by Ruff without changing behavior.
- 2026-06-22: Marked OpenSpec section 7 tasks complete and updated `CODEXNEO_IMPLEMENTATION_LIST.md` statuses for CN-005, CN-006, and CN-007 to Verified.
- 2026-06-22: Per user instruction, left the existing `127.0.0.1:2455` Codex/codex-lb process running untouched; used a temporary `127.0.0.1:2460` server for read-only CN-005/CN-006/CN-007 smoke checks, then stopped only that temporary PID.
- 2026-06-22: Added per-row CN-007 account action buttons for Use API, Switch, and Switch & Restart after a frontend regression test exposed that only selected-account toolbar actions were present.
- 2026-06-22: Diagnosed a live Save Codex Home failure where the built frontend called `PUT /api/codexneo/codex-home` but the running `127.0.0.1:2455` backend still had the older CodexNeo route table. Restarted only the native codex-lb server process on port 2455, not any Codex account action, then verified the Codex Home route was registered and saving `C:\Users\rcgok\.codex` returned 200.
- 2026-06-23: Added CN-008 to the implementation list after the user selected two-way sync between CodexNeo and the main Accounts tab. The intended model is CodexNeo imports also populate codex-lb's encrypted Accounts database, and Accounts-tab imports also register safe snapshots into the configured Codex Home for CodexNeo/native Codex use.
- 2026-06-23: Added CN-009 and CN-010 to the implementation list from the latest screenshot. CN-009 makes the Activity log a fixed-height internal scroll box and records the current coverage finding: `/v1/*` is logged as OpenAI API, `/api/*` is logged as Management API, `/api/codexneo/activity-log` is skipped to prevent recursion, and non-API/static/page events are not logged. CN-010 removes the red-boxed `API`, `API h`, `API d`, `Avail` columns and row-level `Use API` buttons while keeping row Switch/Switch & Restart.
- 2026-06-23: Started CN-008/CN-009/CN-010 implementation by updating OpenSpec requirements/tasks, adding RED backend/frontend tests, wiring CodexNeo account import/delete sync to the encrypted Accounts database, wiring Accounts-tab import/delete sync back to the configured Codex Home, and removing the red-boxed CodexNeo table columns/row Use API button.
- 2026-06-23: Hardened Accounts-tab Codex Home sync so an unexpected `codex-auth` command failure reports a safe sync failure instead of failing the primary Accounts import/delete operation.
- 2026-06-23: Started CN-011 after confirming the user's requirements: Import file/folder/export-all should use backend-native Windows pickers when no manual path is supplied; Codex/Backup columns should be checkboxes; Codex all and Backup all should persist sticky defaults for future accounts while preserving manually unticked rows until the bulk default is toggled off and on; Backup off deletes backups; Delete selected removes accounts from both Codex Home and Backup. Updated OpenSpec section 9 and the implementation list before code.
- 2026-06-23: Added the standing restart rule to repo guidance and CodexNeo notes: restart the codex-ib/codex-lb server automatically when a code reload is needed; ask before restarting the Codex desktop app.
- 2026-06-23: Completed CN-014 for the CodexNeo table/action correction: removed the selected-account Use API route/client/hook/button, replaced the toolbar action with Refresh selected, restored `Avail`, and formatted `Status / Last` from usage status plus relative last activity. Live registry-present accounts now show `Ready` even when the auth snapshot checkbox state is false.
- 2026-06-23: Diagnosed the live Import file error as a stale `127.0.0.1:2455` codex-lb server still running the older backend/static app. Restarted only the codex-ib/codex-lb server, not the Codex desktop app, and verified the live page/API now expose CN-011 fields and controls.
- 2026-06-23: Diagnosed the long-running Import folder overlay as a hidden backend-launched Windows `FolderBrowserDialog` from `powershell.exe` PID 46564. Stopped that picker process only, then started CN-012 to replace empty-path import buttons with browser file/folder upload flows and safe non-blocking backend empty-path responses.
- 2026-06-23: Added RED CN-012 backend tests for `/api/codexneo/import/file-upload` and `/api/codexneo/import/folder-upload`; both initially returned 405. Added RED frontend test for hidden browser import inputs; it initially failed because `Choose import file` was missing.
- 2026-06-23: Implemented CN-012 upload routes, removed server-side import picker fallback from the import/export service, and wired empty-path Import file/folder buttons to browser file/folder inputs while preserving typed manual path imports.
- 2026-06-23: Completed CN-012 verification and restarted only the native codex-lb server. Live `127.0.0.1:2455` now exposes upload import routes, the CodexNeo page contains browser import file/folder inputs, and no server-side picker process remains running.
- 2026-06-23: Diagnosed Export all error. Live `POST /api/codexneo/export/all` returned `internal_error`; direct runner evidence showed Python subprocess could not launch bare `codex-auth`, while PowerShell found `codex-auth.cmd`, and installed `codex-auth 0.2.10` does not support an `export` command. Started CN-013 to make Export all native and to resolve the Windows command shim for remaining command-backed actions.
- 2026-06-23: Added CN-013 RED tests for native Export all copying Codex Home plus Backup auth snapshots and for resolving the Windows `codex-auth.cmd` shim.
- 2026-06-23: Implemented CN-013 backend changes: Export all now copies known `*.auth.json` snapshots directly into a timestamped export directory, and the command runner resolves the actual `codex-auth` executable/shim before launching import/switch/delete commands.
- 2026-06-23: Completed CN-013 verification and restarted only the native codex-lb server. Live Export all now returns success and writes timestamped auth snapshot exports under the codex-lb data directory.
- 2026-06-24: Started CN-015 after user clarified the model: CodexGO/native Codex owns adding auth files to configured Codex Home, Codex IB must not add a Codex login/device-login flow, and CodexNeo refresh should sync valid Codex Home/Backup auth snapshots into Codex IB Accounts so the Codex IB API can use them. Updated OpenSpec, task list, implementation list, and added `cn-015-implementation-plan.md` before code.
- 2026-06-24: Added CN-015 RED tests for Codex Home snapshot discovery, root `auth.json` discovery, backup-only discovery, and `GET /api/codexneo/accounts` syncing into the main Accounts tab. Focused pytest failed as expected: `CodexNeoAccountsSyncService.sync_codex_home_to_accounts` is missing, and the Accounts tab remains empty after CodexNeo account refresh.
- 2026-06-24: Added CN-015 RED test for CodexGO use/refresh sync injection. Focused pytest failed as expected because `CodexNeoService` does not yet accept an account sync dependency or append Accounts sync status after writing auth.
- 2026-06-24: Implemented CN-015 backend sync: CodexNeo now scans root `auth.json`, managed Codex Home account snapshots, parseable account JSON, and app-owned Backup snapshots; imports valid snapshots into the encrypted Accounts database through `AccountsService`; backs up live managed snapshots while preserving manual backup-disabled overrides; runs sync from `GET /api/codexneo/accounts`; and runs sync after CodexGO auth writes. Focused CN-015 pytest passed: 5 passed.
- 2026-06-24: Completed CN-015 verification. Focused CodexNeo backend regression suite passed 39 tests; Ruff passed; `openspec validate add-codexneo-windows-integration --strict` passed; `git diff --check` passed with only LF-to-CRLF warnings on existing touched files; restarted only native codex-lb on port 2455, with final listener PID 48404; live smoke showed `GET /api/codexneo/accounts` returns 3 CodexNeo rows and `GET /api/accounts` returns 4 Accounts rows without printing token data.
- 2026-06-24: Started CN-016 after user reported Refresh auth says 4 accounts but UI shows 3 and circled two layout issues. Live evidence showed `GET /api/codexneo/accounts` now returns 4 rows while the screenshot was stale, and `GET /api/accounts` had repeated `__copy` duplicate rows for the same upstream ChatGPT identities. Updated OpenSpec and implementation list before code.
- 2026-06-24: Added CN-016 RED tests only: backend discovery-sync idempotency with `import_without_overwrite=True`, frontend CodexGO auth query invalidation, and component layout assertions for the selected-action toolbar/table row action wrapping.
- 2026-06-24: Implemented CN-016 green path: `AccountsService.import_account()` now accepts an explicit duplicate-preservation override, automatic Codex Home discovery calls it with `preserve_unknown_workspace_duplicates=False`, CodexGO Use/Refresh invalidates CodexNeo and main Accounts queries, and the CodexNeo selected-account toolbar/table rows now use stable layout hooks/classes.
- 2026-06-24: Completed CN-016 verification and restarted only native codex-lb, not Codex Desktop. Live `/api/codexneo/accounts` returns 4 registry rows; rendered Chrome smoke shows all 4 rows after scrolling, the validity-date input grouped with selected actions, and one-line row Switch/Switch & Restart buttons.
- 2026-06-24: Started full post-implementation bug audit. Confirmed two regressions with RED tests: selected export skipped backup-only accounts, and CodexNeo delete synced the main Accounts database after `codex-auth remove`, which can delete the live auth snapshot before sync can match the account.
- 2026-06-24: Fixed the audit regressions by making selected export search both live Codex Home and app-owned Backup snapshots, and by staging delete auth snapshots before `codex-auth remove` so Accounts sync runs only after a successful external remove while still having matching auth claims available.
- 2026-06-24: Added and fixed a delete safety regression proving CodexNeo must not delete matching Accounts rows if `codex-auth remove` fails.
- 2026-06-24: Recorded the audit work as CN-017 in `CODEXNEO_IMPLEMENTATION_LIST.md`, added OpenSpec task section 15, and tightened the CodexNeo import/export plus two-way sync requirements for backup-only selected export and staged delete sync.
- 2026-06-24: Live API smoke exposed a third CN-017 defect: real Codex auth snapshots in `C:\Users\rcgok\.codex\accounts` use URL-safe base64-encoded account-key filenames, while the implementation only looked for raw `<account_key>.auth.json`. Added RED tests and fixed snapshot lookup through a shared helper used by discovery, location toggles, import/export, delete staging, and Accounts sync.
- 2026-06-24: Diagnosed the visible CodexNeo `Unexpected error` banner. Live endpoint checks and Chrome network capture showed all page-load APIs returning 200, with no fresh alert on a clean load. Root cause was frontend state: the CodexNeo page included old mutation/action errors in the persistent page-level alert. Added RED frontend regression and changed the alert to show query/load errors only; action failures continue to surface through toasts.

## Verification Log

- RED: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py -q` fails with `ModuleNotFoundError: No module named 'app.modules.codexneo'`, confirming the tests target missing behavior.
- Note: `uv` is not currently on this PowerShell PATH; focused pytest checks are using the repo virtualenv Python directly.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py -q` passes: 8 passed.
- GREEN: `.venv\Scripts\python.exe -c "import app.main; print('app.main import ok')"` passes.
- RED: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx src/components/layout/app-header.codexneo.test.tsx` fails because `./codexneo-page` is missing and the header lacks a `CodexNeo` link.
- Note: `bun` is not currently on this PowerShell PATH; focused Vitest checks are using local Vitest through Node.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx src/components/layout/app-header.codexneo.test.tsx` passes: 2 files, 4 tests.
- GREEN: `.venv\Scripts\ruff.exe check app/modules/codexneo tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py` passes.
- GREEN: `node node_modules/typescript/bin/tsc -b` passes.
- GREEN: `node node_modules/vite/bin/vite.js build` passes and writes built assets to `app/static`; Vite reports the existing large chunk warning.
- GREEN: Playwright browser smoke confirmed the `CodexNeo` nav link, default Codex API URL, CodexGO provider URL, refresh interval, empty buyer token field, and action buttons render with no console errors.
- CLEANUP: stopped temporary uvicorn PID 14300 and removed `codexneo-smoke-uvicorn.log` / `codexneo-smoke-uvicorn.err.log`.
- GREEN: `git diff --check` reports no whitespace errors; it only reports LF-to-CRLF warnings for existing Windows checkout behavior on touched files.
- GREEN: `openspec validate --specs` passes: 30 specs, 0 failures.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after CN-002/CN-003 spec updates.
- RED: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_activity_log.py tests/unit/test_codexneo_accounts.py tests/unit/test_codexneo_service.py -q` fails during collection because `app.modules.codexneo.activity_log` and `app.modules.codexneo.accounts` do not exist yet.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_activity_log.py tests/unit/test_codexneo_accounts.py tests/unit/test_codexneo_service.py -vv` passes: 9 passed.
- RED: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` fails because the CodexNeo page does not yet render `OpenAI log`, `Codex Home Accounts`, or `Activity log`.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` passes: 1 file, 5 tests.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after CN-002/CN-003 implementation.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after CN-004 restart scenarios/tasks were added.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_activity_log.py tests/unit/test_codexneo_accounts.py tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py -q` passes: 11 passed.
- GREEN: `.venv\Scripts\ruff.exe check app/modules/codexneo tests/unit/test_codexneo*.py` passes.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx src/components/layout/app-header.codexneo.test.tsx` passes: 2 files, 6 tests.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_restart.py -vv` passes: 4 passed.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_activity_log.py tests/unit/test_codexneo_accounts.py tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py tests/unit/test_codexneo_restart.py -q` passes: 15 passed.
- GREEN: `.venv\Scripts\ruff.exe check app/modules/codexneo tests/unit/test_codexneo*.py` passes.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx src/components/layout/app-header.codexneo.test.tsx` passes: 2 files, 6 tests.
- GREEN: `node node_modules/typescript/bin/tsc -b` passes.
- GREEN: `node node_modules/vite/bin/vite.js build` passes and reports only the existing large chunk warning.
- GREEN: Read-only API smoke confirmed `/health`, `/api/codexneo`, and `/api/codexneo/accounts` are reachable without invoking restart-triggering actions.
- GREEN: Read-only Playwright smoke confirmed `/codexneo` renders with no console warnings/errors.
- GREEN: `node node_modules/typescript/bin/tsc -b` passes.
- GREEN: `node node_modules/vite/bin/vite.js build` passes and updates `app/static`; Vite still reports the existing large chunk warning.
- GREEN: Restarted the native server; `http://127.0.0.1:2455/health` returns 200 and the current listener PID is 58472.
- GREEN: Playwright fallback browser smoke on `http://127.0.0.1:2455/codexneo` confirmed the CodexNeo nav/page, OpenAI log and Management log switches, Codex Home Accounts table, Activity log panel, Clear button, and no console warnings/errors.
- GREEN: API smoke confirmed `/api/codexneo` exposes both activity-log toggles, `/api/codexneo/accounts` returns account rows, and `/api/codexneo/activity-log` is readable without exposing token values.
- GREEN: Restart stopped old PID 54764 and launched a new server; the final listener on `127.0.0.1:2455` is PID 38032. `http://127.0.0.1:2455/health` and `http://127.0.0.1:2455/api/codexneo` both return 200.
- GREEN: Playwright browser smoke on `http://127.0.0.1:2455/codexneo` confirmed the CodexNeo heading/nav, default Codex API URL, CodexGO provider URL, interval, empty buyer token field, and no console errors.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_activity_log.py tests/unit/test_codexneo_accounts.py tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py tests/unit/test_codexneo_restart.py tests/unit/test_codexneo_home_ops.py tests/unit/test_codexneo_import_export_actions.py -q` passes: 20 passed.
- GREEN: `.venv\Scripts\ruff.exe check app/modules/codexneo tests/unit/test_codexneo*.py` passes after CN-005/CN-006/CN-007 cleanup.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx src/components/layout/app-header.codexneo.test.tsx` passes: 2 files, 8 tests.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after CN-005/CN-006/CN-007 route/UI work.
- GREEN: `node node_modules/typescript/bin/tsc -b` passes after CN-005/CN-006/CN-007 frontend changes.
- GREEN: `node node_modules/vite/bin/vite.js build` passes and reports the existing large chunk warning.
- NOTE: Existing native server at `127.0.0.1:2455` returned 404 for `/api/codexneo/codex-home`, indicating it was still running older code; it was not restarted because the user explicitly said not to stop Codex during testing.
- GREEN: Temporary read-only smoke server on `127.0.0.1:2460` returned `/health` ok, `/api/codexneo/codex-home` with `C:\Users\rcgok\.codex`, and `/api/codexneo/accounts` with 6 account rows.
- GREEN: In-app browser smoke on `http://127.0.0.1:2460/codexneo` confirmed Codex Home controls, import/export controls, selected-account actions, and Activity log render with zero console warnings/errors.
- GREEN: Non-destructive browser interaction selected one account checkbox and confirmed `Export selected` and `Delete selected` became enabled without invoking any account action endpoint.
- CLEANUP: Closed the smoke browser tab, stopped temporary PID 32268 on port 2460, and removed the temporary smoke logs.
- RED: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` failed because row-level Use API/Switch/Switch & Restart buttons were missing for `backup@example.com`.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` passes after adding the row action column: 1 file, 8 tests.
- RED: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_import_export_actions.py tests/integration/test_codexneo_accounts_sync_api.py -q` initially failed for missing CN-008 sync injection points and missing Accounts API sync dependency.
- RED: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` initially failed for the still-visible API table columns, missing activity-log scroll test id, and still-visible row-level Use API action.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_import_export_actions.py tests/integration/test_codexneo_accounts_sync_api.py -q` passes after CN-008 backend sync implementation: 7 passed.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` passes after CN-009/CN-010 UI implementation: 1 file, 8 tests.
- RED: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_import_export_actions.py tests/unit/test_codexneo_locations.py -q` fails because `app.modules.codexneo.locations` does not exist yet, confirming CN-011 location behavior is missing.
- RED: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` fails because `Codex all` / `Backup all` controls are not rendered yet, confirming CN-011 frontend behavior is missing.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_import_export_actions.py tests/unit/test_codexneo_locations.py -q` passes after adding picker fallback and Codex/Backup location service behavior: 11 passed.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` passes after adding Codex/Backup row checkboxes and Codex all/Backup all controls: 1 file, 9 tests.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after CN-011 spec/tasks updates.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_activity_log.py tests/unit/test_codexneo_accounts.py tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py tests/unit/test_codexneo_restart.py tests/unit/test_codexneo_home_ops.py tests/unit/test_codexneo_import_export_actions.py tests/unit/test_codexneo_locations.py tests/integration/test_codexneo_accounts_sync_api.py -q` passes: 30 passed.
- GREEN: `.venv\Scripts\ruff.exe check app/modules/codexneo app/modules/accounts/api.py app/modules/accounts/schemas.py tests/unit/test_codexneo*.py tests/integration/test_codexneo_accounts_sync_api.py` passes.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx src/components/layout/app-header.codexneo.test.tsx` passes: 2 files, 10 tests.
- GREEN: `node node_modules/typescript/bin/tsc -b` passes.
- GREEN: `node node_modules/vite/bin/vite.js build` passes and reports only the existing large chunk warning.
- GREEN: `git diff --check` passes with only LF-to-CRLF working-copy warnings from Git on touched files.
- GREEN: Non-destructive temporary server smoke on `127.0.0.1:2460` returned `/health` 200 and `/api/codexneo/accounts` 200 with `codexAllEnabled` / `backupAllEnabled` fields present.
- GREEN: Chrome-channel Playwright smoke on `http://127.0.0.1:2460/codexneo` confirmed `Codex all`, `Backup all`, row Codex/Backup checkboxes, and Import file/Import folder/Export all buttons render with no console warnings/errors. Native picker buttons were not clicked during smoke to avoid modal dialogs.
- CLEANUP: Stopped only the temporary `127.0.0.1:2460` smoke server and removed `.tmp-codexneo-smoke-data`; the existing live Codex/codex-lb listener was not stopped or restarted during CN-011.
- GREEN: Live stale-server fix: before restart, `http://127.0.0.1:2455/api/codexneo/accounts` omitted `codexAllEnabled` / `backupAllEnabled`, and `/codexneo` did not expose the new UI marker. Restarted codex-lb from `.venv\Scripts\codex-lb.exe`; final live listener is PID 63244, `/health` returns 200, `/api/codexneo/accounts` returns `codexAllEnabled=False` and `backupAllEnabled=False`, and Chrome smoke on `http://127.0.0.1:2455/codexneo` confirms `Codex all`, `Backup all`, `Import file`, 4 account rows, and no console warnings/errors.
- RED: `.venv\Scripts\python.exe -m pytest tests/integration/test_codexneo_upload_import_api.py -q` initially failed with 405 Method Not Allowed for missing upload import routes.
- RED: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx` initially failed to find the `Choose import file` input, confirming the empty-path import buttons were still posting to backend path routes.
- GREEN: `.venv\Scripts\python.exe -m pytest tests\integration\test_codexneo_upload_import_api.py tests\unit\test_codexneo_import_export_actions.py -q` passes: 9 passed.
- GREEN: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx` passes: 1 file, 10 tests.
- GREEN: `.venv\Scripts\ruff.exe check app\modules\codexneo tests\unit\test_codexneo*.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_accounts_sync_api.py` passes.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes.
- GREEN: `node node_modules\typescript\bin\tsc -b` exits 0.
- GREEN: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py -q` passes: 32 passed.
- GREEN: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx src\components\layout\app-header.codexneo.test.tsx` passes: 2 files, 11 tests.
- GREEN: `node node_modules\vite\bin\vite.js build` passes and reports only the existing large-chunk warning.
- GREEN: `git diff --check` exits 0 with only LF-to-CRLF working-copy warnings on existing touched files.
- GREEN: Restarted only the native codex-lb server, not Codex Desktop. Final clean restart listener PID is 47424, `/health` returns 200, OpenAPI exposes `/api/codexneo/import/file-upload` and `/api/codexneo/import/folder-upload`, and `GET /codexneo` returns 200.
- GREEN: Chrome-channel Playwright smoke on `http://127.0.0.1:2455/codexneo` confirms the CodexNeo page renders with one `Choose import file` input, one `Choose import folder` input, and Import folder controls; only Chrome's existing password-field form warning appeared.
- GREEN: Process check found no remaining `FolderBrowserDialog`, `OpenFileDialog`, or `Choose a folder containing auth JSON files` picker process after the fix.
- RED: Live `Invoke-RestMethod POST http://127.0.0.1:2455/api/codexneo/export/all` returned `{ "error": { "code": "internal_error", "message": "Unexpected error" } }` before CN-013.
- RED: `codex-auth export --help` returned `error: unknown command 'export'`, confirming Export all cannot depend on the installed CLI.
- RED: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_import_export_actions.py::test_export_all_uses_timestamped_directory_and_selected_export_copies_safe_files tests\unit\test_codexneo_command_runner.py -q` failed because Export all wrote no snapshots and `command_runner.shutil` did not exist.
- GREEN: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_command_runner.py -q` passes: 8 passed.
- GREEN: `.venv\Scripts\ruff.exe check app\modules\codexneo tests\unit\test_codexneo*.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_accounts_sync_api.py` passes after CN-013 focused backend changes.
- GREEN: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_command_runner.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py -q` passes: 33 passed.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after the Export all native snapshot spec update.
- GREEN: `git diff --check` exits 0 with only LF-to-CRLF working-copy warnings on existing touched files.
- GREEN: Restarted only the native codex-lb server, not Codex Desktop. Final listener PID is 69968 and `/health` returns 200.
- GREEN: Live `POST http://127.0.0.1:2455/api/codexneo/export/all` with empty path returns `success=true`, message `Exported 4 account auth snapshot(s)`, and writes 4 `*.auth.json` files to `C:\Users\rcgok\.codex-lb\codexneo-export-20260623-035111-266367`.
- GREEN: `.venv\Scripts\python.exe -c "import asyncio; from app.modules.codexneo.command_runner import run_codex_auth_command; print(asyncio.run(run_codex_auth_command(['--version'], codex_home=r'C:\Users\rcgok\.codex')))"` returns `(True, 'codex-auth 0.2.10')`, confirming Python can launch the resolved Windows shim.
- RED: Focused CN-014 frontend test initially failed because the row `Backup` availability value conflicted with the `Backup` column header query; scoped the assertion to the backup account row.
- GREEN: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_import_export_actions.py -q` passes: 9 passed.
- GREEN: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx` passes: 1 file, 10 tests.
- GREEN: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_command_runner.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py -q` passes after the live availability correction: 34 passed.
- GREEN: `.venv\Scripts\ruff.exe check app\modules\codexneo tests\unit\test_codexneo*.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_accounts_sync_api.py` passes.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after the deprecated Use API scenario was removed from the spec.
- GREEN: `node node_modules\typescript\bin\tsc -b` exits 0.
- GREEN: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx src\components\layout\app-header.codexneo.test.tsx` passes: 2 files, 11 tests.
- GREEN: `node node_modules\vite\bin\vite.js build` passes with only the existing large-chunk warning.
- GREEN: `git diff --check` exits 0 with only LF-to-CRLF working-copy warnings on existing touched files.
- GREEN: Restarted only the native codex-lb server, not Codex Desktop. Final live listener PID is 61912 and `/health` returns `ok`.
- GREEN: Live API smoke on `http://127.0.0.1:2455/api/codexneo/accounts` returns two account rows with `Avail=Ready` and formatted statuses like `Unknown / just now`; no raw timestamp is displayed as the status cell value.
- GREEN: Live OpenAPI smoke confirms `/api/codexneo/accounts/use-api` is absent and `/api/codexneo/accounts` remains present.
- GREEN: Chrome-channel DOM smoke on `http://127.0.0.1:2455/codexneo` confirms the table has an `Avail` header, selected toolbar has `Refresh selected`, there is no `Use API` button, status cells contain ` / ` formatted text, and there are no console warnings/errors.
- GREEN: `openspec validate add-codexneo-windows-integration --strict` passes after CN-008/CN-009/CN-010 spec updates.
- GREEN: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_activity_log.py tests/unit/test_codexneo_accounts.py tests/unit/test_codexneo_service.py tests/unit/test_codexneo_scheduler.py tests/unit/test_codexneo_restart.py tests/unit/test_codexneo_home_ops.py tests/unit/test_codexneo_import_export_actions.py tests/integration/test_codexneo_accounts_sync_api.py -q` passes: 24 passed.
- GREEN: `node node_modules/vitest/vitest.mjs run src/features/codexneo/components/codexneo-page.test.tsx src/components/layout/app-header.codexneo.test.tsx` passes: 2 files, 9 tests.
- FIXED: Ruff found one long line in `app/modules/codexneo/import_export.py`; wrapped the import-folder command call without behavior changes.
- GREEN: `.venv\Scripts\ruff.exe check app/modules/codexneo app/modules/accounts/api.py app/modules/accounts/schemas.py tests/unit/test_codexneo*.py tests/integration/test_codexneo_accounts_sync_api.py` passes.
- GREEN: `node node_modules/typescript/bin/tsc -b` passes.
- GREEN: `node node_modules/vite/bin/vite.js build` passes and reports only the existing large chunk warning.
- GREEN: `git diff --check` passes with only LF-to-CRLF working-copy warnings from Git on touched files.
- GREEN: Non-destructive temporary server smoke on `127.0.0.1:2460` returned `/health` 200, `/api/codexneo` 200, and `/api/codexneo/accounts` 200 with 4 discovered account rows from `C:\Users\rcgok\.codex\accounts\registry.json`.
- GREEN: Chrome-channel Playwright smoke on `http://127.0.0.1:2460/codexneo` confirmed the CodexNeo page renders, `API`/`API h`/`API d`/`Avail` headers are absent, row-level `Use API` buttons are absent, row Switch/Switch & Restart remain, Activity log has `max-height: 320px` and `overflow-y: auto`, and there are no console warnings/errors.
- CLEANUP: Stopped only the temporary `127.0.0.1:2460` smoke server and removed `.tmp-codexneo-smoke-data`; the existing live Codex/codex-lb listener was not stopped or restarted during this pass.
- GREEN: Final verification rerun after row actions: backend CodexNeo pytest suite 20 passed; Ruff passed; CodexNeo/header Vitest 2 files, 9 tests passed; OpenSpec strict validation passed; TypeScript build passed; Vite production build passed with the existing large chunk warning.
- GREEN: Final temporary read-only smoke server on `127.0.0.1:2460` returned `/health` ok, Codex Home `C:\Users\rcgok\.codex`, and 6 account rows; in-app browser confirmed all CN-005/CN-006/CN-007 controls plus row Use API/Switch/Switch & Restart buttons with zero console warnings/errors.
- GREEN: Final non-destructive browser interaction selected one account checkbox and confirmed `Export selected` and `Delete selected` enabled without invoking side-effecting endpoints.
- CLEANUP: Closed the final smoke browser tab, stopped temporary PID 11072 on port 2460, and removed final smoke logs.
- GREEN: Live stale-backend diagnosis: before refresh, `PUT http://127.0.0.1:2455/api/codexneo/codex-home` returned 405 and `GET` returned 404 while the current checkout registered both methods in-process.
- GREEN: Isolated temporary server with separate `CODEX_LB_DATA_DIR` confirmed `PUT /api/codexneo/codex-home` and `GET /api/codexneo/codex-home` both returned 200 from the current checkout.
- GREEN: Restarted only the native codex-lb listener from `.venv\Scripts\codex-lb.exe`; final listener on `127.0.0.1:2455` is PID 9140, `/health` returns 200, OpenAPI includes `PUT /api/codexneo/codex-home`, and live `PUT`/`GET` with `C:\Users\rcgok\.codex` both return 200.
- RED CN-016: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_account_discovery_sync.py::test_discovery_sync_is_idempotent_when_import_without_overwrite_is_enabled -q` initially failed with duplicate `acc_repeat_discovery_...__copy2`.
- RED CN-016: `node node_modules\vitest\vitest.mjs run src\features\codexneo\hooks\use-codexneo.test.tsx src\features\codexneo\components\codexneo-page.test.tsx` initially failed because CodexGO auth mutations did not invalidate queries and the layout test IDs/classes were absent.
- GREEN CN-016 focused backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_account_discovery_sync.py::test_discovery_sync_is_idempotent_when_import_without_overwrite_is_enabled -q` passes: 1 passed.
- GREEN CN-016 focused frontend: `node node_modules\vitest\vitest.mjs run src\features\codexneo\hooks\use-codexneo.test.tsx src\features\codexneo\components\codexneo-page.test.tsx` passes: 2 files, 12 tests.
- GREEN CN-016 backend slice: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_account_discovery_sync.py tests\integration\test_codexneo_account_discovery_sync_api.py tests\unit\test_codexneo_service.py -q` passes: 12 passed.
- GREEN CN-016 Ruff: `.venv\Scripts\python.exe -m ruff check app\modules\codexneo app\modules\accounts\service.py tests\unit\test_codexneo_account_discovery_sync.py tests\integration\test_codexneo_account_discovery_sync_api.py` passes.
- GREEN CN-016 OpenSpec: `openspec validate add-codexneo-windows-integration --strict` passes.
- GREEN CN-016 TypeScript: `node node_modules\typescript\bin\tsc -b` exits 0.
- GREEN CN-016 build: `node node_modules\vite\bin\vite.js build` passes and reports only the existing large-chunk warning.
- GREEN CN-016 server restart: restarted only the native codex-lb Python server on port 2455; final listener child PID observed as 17148; `/api/codexneo/accounts` returned 4 rows and `Loaded 4 account(s) from Codex registry`.
- GREEN CN-016 rendered smoke: in-app Browser runtime failed before attach due `sandboxCwd must use the file URI scheme`; fallback Chrome-channel Playwright smoke on `http://127.0.0.1:2455/codexneo` had zero console errors, found 4 account rows in the DOM, verified `min-w-[72rem]`, `flex-nowrap` row actions, validity-date control inside the selected-action toolbar, and bottom-scroll screenshot showed all 4 rows.
- RED audit: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_import_export_actions.py::test_export_all_uses_timestamped_directory_and_selected_export_copies_safe_files -q` failed because selected export returned `success=False` after exporting only the live Codex account and skipping the backup-only selected account.
- RED audit: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_import_export_actions.py::test_delete_accounts_syncs_accounts_before_codex_auth_remove_deletes_snapshots -q` failed because the fake remove command deleted the live snapshot before Accounts sync inspected it.
- RED audit: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_import_export_actions.py::test_delete_accounts_does_not_sync_accounts_when_codex_auth_remove_fails -q` failed because Accounts sync still ran before the external remove failure was known.
- RED audit: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_accounts.py::test_registry_account_discovery_matches_encoded_codex_auth_snapshot_filename tests\unit\test_codexneo_import_export_actions.py::test_export_selected_finds_codex_auth_encoded_snapshot_filename -q` failed because encoded real Codex snapshot filenames were not matched to raw registry account keys.
- GREEN audit: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_command_runner.py tests\unit\test_codexneo_account_discovery_sync.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_account_discovery_sync_api.py -q` passes after encoded snapshot support: 44 passed.
- GREEN audit final backend/API: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_command_runner.py tests\unit\test_codexneo_account_discovery_sync.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_account_discovery_sync_api.py tests\integration\test_accounts_api.py tests\integration\test_accounts_api_extended.py tests\integration\test_account_auth_export.py tests\integration\test_account_opencode_auth_export.py -q` passes: 97 passed.
- GREEN audit final frontend: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx src\features\codexneo\hooks\use-codexneo.test.tsx src\components\layout\app-header.codexneo.test.tsx` passes: 3 files, 13 tests.
- GREEN audit final quality: Ruff passes; `openspec validate add-codexneo-windows-integration --strict` passes; `node node_modules\typescript\bin\tsc -b` exits 0; `node node_modules\vite\bin\vite.js build` passes with only the existing large-chunk warning; `git diff --check` exits 0 with only LF-to-CRLF warnings.
- GREEN audit final live: restarted only the native codex-lb server, not Codex Desktop. Final `/health` is ok, `/api/codexneo/accounts` returns 16 rows with 16 `codex=true` and 16 `backup=true`, selected export for a real encoded-filename account returns `success=true`, and Chrome-channel rendered smoke shows 16 table rows, 16 Codex checked, 16 Backup checked, activity log `overflow-y: auto`, and no console warnings/errors.
- RED runtime banner: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx -t "does not show stale action errors"` failed because `Unexpected error` from `exportSelectedMutation.error` rendered in the persistent page alert even though queries were successful.
- GREEN runtime banner: the same focused Vitest test now passes; CodexNeo/header frontend suite passes 14 tests; TypeScript exits 0; Vite build passes with only the existing large-chunk warning; backend focused CodexNeo account/export tests pass 14 tests.
- GREEN runtime live: restarted only native codex-lb, not Codex Desktop. Final listener health is ok; Chrome-channel smoke on `http://127.0.0.1:2455/codexneo` shows no page alert, 16 rows, no console warnings/errors; live `/api/codexneo/accounts` returns 16 rows with 16 `codex=true` and 16 `backup=true`.
- CURRENT: `CODEXNEO_IMPLEMENTATION_LIST.md` tracks requested CodexNeo work through CN-025, and all listed items are verified.
- RED CN-018 backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_health.py tests\integration\test_codexneo_health_api.py -q` initially failed with `ModuleNotFoundError: No module named 'app.modules.codexneo.health'`.
- RED CN-018 frontend: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx -t "health diagnostics"` initially failed because the CodexNeo page did not render a `Health diagnostics` panel.
- GREEN CN-018 focused backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_health.py tests\integration\test_codexneo_health_api.py -q` passes: 3 passed.
- GREEN CN-018 backend regression: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_health.py tests\integration\test_codexneo_health_api.py tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_command_runner.py tests\unit\test_codexneo_account_discovery_sync.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_account_discovery_sync_api.py -q` passes: 47 passed.
- GREEN CN-018 frontend: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx src\features\codexneo\hooks\use-codexneo.test.tsx src\components\layout\app-header.codexneo.test.tsx` passes: 3 files, 15 tests.
- GREEN CN-018 quality: `.venv\Scripts\ruff.exe check app\modules\codexneo tests\unit\test_codexneo*.py tests\integration\test_codexneo*.py` passes; `openspec validate add-codexneo-windows-integration --strict` passes; `node node_modules\typescript\bin\tsc -b` exits 0; `node node_modules\vite\bin\vite.js build` passes with only the existing large-chunk warning; `git diff --check` exits 0 with only LF-to-CRLF warnings.
- GREEN CN-018 live: restarted only the native codex-lb server, not Codex Desktop. Final listener on `127.0.0.1:2455` returns `/health` ok and `/api/codexneo/health` returns `overallStatus=ok` with 8 badges: Codex IB, Codex Home, Codex registry, Backup store, Accounts sync, Activity log, CodexGO auth, and OpenAI bridge.
- GREEN CN-018 rendered smoke: Browser plugin attach failed with `sandboxCwd must use the file URI scheme`, so Chrome-channel Playwright fallback checked `http://127.0.0.1:2455/codexneo` after the production build. The page showed all 8 diagnostics labels, 7 safe copy buttons, 0 console issues, and 0 alert banners.
- CURRENT: CN-018 is implemented. CodexNeo now has safe health diagnostics for Codex IB, Codex Home, registry/snapshots, app-owned backup store, Accounts sync, activity-log streams, CodexGO auth configuration, and OpenAI bridge configuration. Safe copy buttons copy paths/URLs only and do not expose buyer tokens or auth JSON contents.
- RED CN-019 backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_locations.py::test_backup_all_follows_visible_state_and_manual_off_disables_future_default tests\unit\test_codexneo_locations.py::test_codex_all_is_not_a_future_restore_default_for_backup_only_accounts tests\unit\test_codexneo_account_discovery_sync.py::test_sync_codex_home_does_not_backup_live_accounts_when_backup_all_is_off -q` initially failed: Backup-all stayed enabled after row Backup off, Codex-all tried to restore an already-live row from missing backup data, and discovery sync created a backup while Backup-all was off.
- GREEN CN-019 focused backend: the same parity tests now pass: 3 passed.
- GREEN CN-019 related backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_account_discovery_sync.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_import_export_actions.py tests\integration\test_codexneo_account_discovery_sync_api.py tests\integration\test_codexneo_accounts_sync_api.py -q` passes: 27 passed.
- CURRENT: CN-019 changed Codex/Backup location behavior to match the Windows app: bulk states are recomputed from visible rows, Codex-all is not a future restore policy, Backup-all is cleared when a row backup is manually removed, and discovery sync only auto-backs up live Codex snapshots when Backup-all is currently enabled.
- GREEN CN-019 final backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_health.py tests\integration\test_codexneo_health_api.py tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_command_runner.py tests\unit\test_codexneo_account_discovery_sync.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_account_discovery_sync_api.py -q` passes: 49 passed.
- GREEN CN-019 frontend/quality: CodexNeo component and hook Vitest tests pass 14 tests; Ruff passes; OpenSpec strict validation passes; TypeScript exits 0; Vite production build passes with only the existing large-chunk warning.
- GREEN CN-019 live: restarted only native codex-lb, not Codex Desktop. Live `/api/codexneo/accounts` returns 16 rows with `codexAllEnabled=true`, `backupAllEnabled=true`, 16 Codex checked, and 16 Backup checked. Chrome-channel rendered smoke confirms the same state on `http://127.0.0.1:2455/codexneo` with 0 console issues and 0 alert banners.
- INTAKE CN-020: User reported Codex Home has about 10 accounts but Codex IB / Accounts / CodexNeo show more because duplicate backup/auth copies of the same real account are treated as separate accounts. Added CN-020 to the implementation list only; no implementation started. Key pending decision: whether existing duplicate Accounts DB rows should be merged/deleted, hidden from UI/API selection, or preserved but marked duplicate, and which history/usage fields become canonical.
- INTAKE CN-021: User reported CodexNeo still shows weak `Status / Last` values such as `Unknown / 1m ago`. Added CN-021 to the implementation list only; no implementation started. Accepted direction: either sync CodexNeo status/usage from Codex LB dashboard/account status data, or use the same Codex API/status refresh path used by the CodexNeo Windows app. The implementation must document source precedence and avoid token exposure.
- INTAKE CN-022: User requested a `Select all` control near Codex all / Backup all that selects all accounts currently shown in the CodexNeo account table. Added CN-022 to the implementation list only; no implementation started. Interpreted as a frontend selection action with no Codex/Backup location side effects.
- INTAKE CN-023: User requested minutes to be shown for the CodexGO Auth refresh interval field currently labeled `Every` with value `30`. Added CN-023 to the implementation list only; no implementation started. Interpreted as a frontend label/unit change with no backend behavior change.
- INTAKE CN-024: User asked whether the top-right Codex Home Accounts `Refresh` button is actually clickable. Live Chrome smoke showed the button is enabled and clicking it sends a new `/api/codexneo/accounts` request with 200 response, but the UI gives no visible confirmation. Added CN-024 to the implementation list only; no implementation started. Goal is refresh UX/feedback and explicit clickability verification.
- 2026-06-25: Started CN-020 through CN-024 after user approved implementing the full pending list. Chose the non-destructive CN-020 cleanup path: dedupe visible/API candidate surfaces by stable identity, without deleting historical Accounts database rows.
- 2026-06-25: Added OpenSpec section 18, RED backend tests for CodexNeo auth-identity dedupe, generated Accounts `__copy` filtering, and status fallback from usage windows, plus RED frontend tests for Select all, `Every (min)`, and Codex Home Accounts refresh feedback.
- 2026-06-25: Implemented CodexNeo account dedupe by parsing safe auth snapshot claims from live Codex Home and app-owned Backup snapshots, merging Codex/Backup location flags, and preferring a live/active row for row actions. Implemented status fallback so Codex-present rows with usage windows show `Fresh` instead of `Unknown`.
- 2026-06-25: Implemented non-destructive Accounts generated-copy filtering in `AccountsRepository.list_accounts()`: generated `__copy` rows are hidden only when upstream identity and plan slot match, preserving existing import-without-overwrite workflows that intentionally show separate plan/workspace rows. CodexNeo delete-sync uses `include_generated_copies=True` so hidden historical rows can still be matched for cleanup.
- 2026-06-25: Implemented CodexNeo frontend polish: Select all selects all visible rows without changing Codex/Backup checkboxes, CodexGO interval label now reads `Every (min)`, and the Codex Home Accounts Refresh button shows `Refreshing...` plus `Last refreshed just now` after a successful refetch.
- 2026-06-25: Completed CN-020 through CN-024 verification and restarted only the native codex-lb server, not Codex Desktop. Final listener is active on `127.0.0.1:2455`; Windows reports the bound child PID as 54736.
- RED CN-020/CN-021 backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_accounts.py::test_registry_account_discovery_deduplicates_same_real_auth_identity tests\unit\test_codexneo_accounts.py::test_registry_account_with_usage_window_without_status_reports_fresh -q` initially failed with 3 rows for one auth identity and `Unknown / just now`.
- RED CN-020 Accounts API: `.venv\Scripts\python.exe -m pytest tests\integration\test_accounts_api.py::test_list_accounts_hides_generated_copy_rows_for_same_identity -q` initially failed because `/api/accounts` showed `acc-generated__copy2`.
- RED CN-022/CN-023/CN-024 frontend: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx -t "selects every visible|shows feedback|renders API"` initially failed because `Every (min)`, `Select all`, and `Last refreshed just now` did not exist.
- GREEN CN-020 through CN-024 broad backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_service.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_command_runner.py tests\unit\test_codexneo_account_discovery_sync.py tests\unit\test_codexneo_health.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_codexneo_account_discovery_sync_api.py tests\integration\test_codexneo_health_api.py tests\integration\test_accounts_api.py tests\integration\test_accounts_api_extended.py tests\integration\test_account_auth_export.py tests\integration\test_account_opencode_auth_export.py tests\unit\test_accounts_repository_locks.py tests\unit\test_account_mappers.py -q` passes: 117 passed.
- GREEN CN-020 through CN-024 frontend/quality: CodexNeo/header Vitest suite passes 17 tests; Ruff passes; OpenSpec strict validation passes; TypeScript exits 0; Vite production build passes with only the existing large-chunk warning; `git diff --check` exits 0 with only LF-to-CRLF warnings.
- GREEN CN-020 through CN-024 live: `/api/codexneo/accounts` returns 20 visible rows with 0 duplicate email groups; `/api/accounts` returns 22 visible rows with 0 visible generated `__copy` rows. Chrome-channel Playwright fallback verified the live CodexNeo page: 20 rows rendered, Select all selected 20 rows, `Every (min)` visible, Refresh showed `Last refreshed just now`, two `/api/codexneo/accounts` responses were 200, and console warnings/errors were 0. Screenshot saved to `C:\Users\rcgok\AppData\Local\Temp\codexneo-cn020-cn024-smoke.png`.
- 2026-06-25: Completed follow-up audit fixes after user clarified Auth->API Set/Revert Codex Desktop restart is intended runtime behavior. Left Set/Revert restart behavior unchanged; fixed equivalent live/backup location actions by resolving auth identity across Codex Home and Backup snapshots; added generated `__copy` Accounts consolidation that merges history/usage into one canonical row; made invalid saved CodexGO intervals safe in settings and health diagnostics; and changed `Refresh selected` to call a selected account usage refresh endpoint instead of only refetching the CodexNeo table.
- 2026-06-25: Completed CN-026 after user reported Delete selected failed with `unexpected extra selector` and requested a CodexNeo/Codex IB master sync. Root cause: both CodexNeo Delete selected and Accounts-tab Codex Home delete passed multiple selectors to `codex-auth remove`, while the installed CLI accepts one selector per command. Fixed both paths to remove one selector at a time, preserving Accounts/Backup cleanup only after successful external remove. Added explicit CodexNeo `Sync` button/API that syncs Codex Home and app-owned Backup snapshots into Codex IB, registers eligible Codex IB accounts back into Codex Home, and writes a token-free `codexneo-master-registry.json` under the codex-lb data directory.
- 2026-06-25: Completed CN-027 after user reported Accounts-page delete failed with `registry schema version 5 is newer than this codex-auth binary supports (max 3)`. Root cause: delete sync still depended on the external `codex-auth remove` binary, and that binary cannot parse the current Codex registry schema. Added a narrow compatibility fallback for that exact schema-version error: Accounts-page delete and CodexNeo Delete selected now remove matching Codex Home and app-owned Backup registry/snapshot entries locally, while unrelated `codex-auth remove` failures still fail safely.
- 2026-06-25: Strengthened CN-027 after user reported CodexNeo Delete selected still surfaced the schema-version toast. The backend now pre-detects configured Codex registry schema v5+ and bypasses `codex-auth remove` before it can fail, while retaining the runtime schema-error fallback for equivalent command output.
- RED follow-up audit: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_locations.py::test_location_actions_resolve_equivalent_live_and_backup_keys tests/integration/test_accounts_repository.py::test_consolidate_generated_copy_duplicates_keeps_active_copy_and_usage tests/unit/test_codexneo_health.py::test_codexneo_health_handles_invalid_refresh_interval_setting tests/unit/test_codexneo_service.py::test_get_settings_handles_invalid_saved_refresh_interval -q` initially failed on exact-key backup removal, missing repository consolidation, and invalid interval crashes.
- GREEN follow-up audit focused backend: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_locations.py::test_location_actions_resolve_equivalent_live_and_backup_keys tests/integration/test_accounts_repository.py::test_consolidate_generated_copy_duplicates_keeps_active_copy_and_usage tests/unit/test_codexneo_health.py::test_codexneo_health_handles_invalid_refresh_interval_setting tests/unit/test_codexneo_service.py::test_get_settings_handles_invalid_saved_refresh_interval tests/unit/test_codexneo_account_discovery_sync.py::test_refresh_selected_account_usage_resolves_codexneo_key -q` passes: 5 passed.
- GREEN follow-up audit broad backend: `$files = @(Get-ChildItem -LiteralPath 'tests\unit' -Filter 'test_codexneo_*.py' | ForEach-Object FullName) + @(Get-ChildItem -LiteralPath 'tests\integration' -Filter 'test_codexneo_*.py' | ForEach-Object FullName) + @('tests\integration\test_accounts_repository.py','tests\integration\test_accounts_api.py'); .\.venv\Scripts\python.exe -m pytest @files -q` passes: 77 passed.
- GREEN follow-up audit frontend: `& 'C:\Users\rcgok\AppData\Local\Microsoft\WinGet\Links\bun.exe' run test src/features/codexneo/components/codexneo-page.test.tsx src/features/codexneo/hooks/use-codexneo.test.tsx src/features/codexneo/api.test.ts` passes: 2 files, 17 tests.
- GREEN follow-up audit quality: `.venv\Scripts\python.exe -m ruff check app/modules/codexneo app/modules/accounts/repository.py app/modules/accounts/service.py tests/unit/test_codexneo_account_discovery_sync.py tests/unit/test_codexneo_health.py tests/unit/test_codexneo_service.py tests/unit/test_codexneo_locations.py tests/integration/test_accounts_repository.py` passes; `& 'C:\Users\rcgok\AppData\Local\Microsoft\WinGet\Links\bun.exe' run typecheck` passes; `npx --yes openspec validate add-codexneo-windows-integration --strict` passes.
- RED CN-026 focused: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_import_export_actions.py::test_delete_accounts_runs_single_remove_per_selected_account_and_deletes_locations tests\unit\test_codexneo_account_discovery_sync.py::test_accounts_delete_removes_matching_codex_home_keys_one_selector_at_a_time -q` initially failed because both delete paths called `remove` with multiple selectors. `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_account_discovery_sync.py::test_sync_all_accounts_writes_master_list_and_reconciles_both_directions -q` initially failed because `sync_all_accounts` was missing. The new route/UI focused tests initially failed with 405/no Sync button/no hook mutation.
- GREEN CN-026 focused: delete, master sync, API route, component, and hook tests pass: 4 backend/API tests plus the focused CodexNeo page/hook Vitest tests.
- GREEN CN-026 broad backend: selected CodexNeo and Accounts regression suite passed: `133 passed in 84.98s`.
- GREEN CN-026 frontend: `& 'C:\Users\rcgok\AppData\Local\Microsoft\WinGet\Links\bun.exe' run test src/features/codexneo/components/codexneo-page.test.tsx src/features/codexneo/hooks/use-codexneo.test.tsx` passes: 2 files, 19 tests.
- GREEN CN-026 quality: `.venv\Scripts\python.exe -m ruff check app\modules\codexneo tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_account_discovery_sync.py tests\integration\test_codexneo_account_discovery_sync_api.py` passes; `npx --yes openspec validate add-codexneo-windows-integration --strict` passes; frontend `bun run typecheck` passes; `bun run build` passes with the existing large-chunk warning; `git diff --check` exits 0 with only existing LF-to-CRLF warnings.
- GREEN CN-026 live: restarted only native codex-lb, not Codex Desktop. Stopped old listener PID 5960 for initial smoke, then restarted again to move server logs outside the repo; final listener PID is 39852 and `/health` returned `ok`. Live OpenAPI contains `/api/codexneo/accounts/sync`; non-mutating rendered Playwright smoke on `http://127.0.0.1:2455/codexneo` found the `Sync` button visible, 20 account rows, and 0 console warnings/errors.
- RED CN-027 focused: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_account_discovery_sync.py::test_accounts_delete_uses_local_remove_when_codex_auth_schema_is_unsupported tests\unit\test_codexneo_import_export_actions.py::test_delete_accounts_uses_local_remove_when_codex_auth_schema_is_unsupported -q` initially failed because both delete paths returned the external schema-version failure.
- GREEN CN-027 focused: the same two tests pass after the local registry/snapshot fallback.
- GREEN CN-027 broad backend: selected CodexNeo and Accounts regression suite passed: `135 passed in 82.14s`.
- GREEN CN-027 quality: `.venv\Scripts\python.exe -m ruff check app\modules\codexneo tests\unit\test_codexneo_account_discovery_sync.py tests\unit\test_codexneo_import_export_actions.py` passes; `npx --yes openspec validate add-codexneo-windows-integration --strict` passes; `git diff --check` exits 0 with only existing LF-to-CRLF warnings.
- GREEN CN-027 live: restarted only native codex-lb, not Codex Desktop. Stopped old listener PID 41620, started listener PID 31048, and `/health` returned `ok`.
- GREEN CN-027 strengthened verification: focused pre-detect/runtime fallback delete tests pass (`4 passed`); broad CodexNeo/Accounts backend suite passes (`137 passed in 99.12s`); Ruff passes; OpenSpec strict validation passes; `git diff --check` exits 0 with only LF-to-CRLF warnings.
- GREEN CN-027 strengthened live reload: restarted only native codex-lb, not Codex Desktop. Stopped old Python listener PID 71512, started `.venv\Scripts\codex-lb.exe` PID 65940, final listener PID 65704, and `/health` returned `ok`. Logs are under `C:\WINDOWS\TEMP\codex-lb-server.out.log` and `C:\WINDOWS\TEMP\codex-lb-server.err.log`.
- 2026-06-25: Completed CN-028 after user reported selecting a Backup-only CodexNeo row and pressing Delete selected appeared to do nothing. Live server logs showed `POST /api/codexneo/accounts/delete` returned 500 because snapshot deletion attempted to unlink an invalid raw Windows path containing `::` before trying the encoded snapshot filename. Fixed snapshot-name candidate generation so unsafe account keys use encoded filenames only, and added a delete mutation error toast so future API failures are visible.
- GREEN CN-028 verification: RED/GREEN backup-only encoded-key delete test passes; RED/GREEN Delete selected error-toast test passes; broad CodexNeo/Accounts backend suite passes (`138 passed in 80.47s`); CodexNeo frontend suite passes (`20 passed`); Ruff passes; TypeScript passes; OpenSpec strict validation passes; `git diff --check` exits 0 with only LF-to-CRLF warnings.
- GREEN CN-028 live reload: production frontend build passed with the existing large-chunk warning; restarted only native codex-lb, not Codex Desktop. Started `.venv\Scripts\codex-lb.exe` PID 54176, final listener PID 66512, and `/health` returned `ok`. Read-only `/api/codexneo/accounts` returned 20 rows, 11 Backup-only rows, and the user-selected `t01038243318@gmail.com` row still listed because it was not deleted by command-line verification.
- INTAKE CN-029: User requested the existing `Select all` control also act as `Unselect all`, optionally represented as a tick/checkbox-style control like the nearby Codex all / Backup all controls. Added to `CODEXNEO_IMPLEMENTATION_LIST.md` only; no implementation started.
- 2026-06-26: Completed CN-029 and CN-030 after user approved implementing pending items and Auth->API config safety. CN-029 changes the existing Select all button into a visible-row Select all / Unselect all toggle without touching Codex/Backup location checkboxes. CN-030 adds a protected `config.toml.codexneo-original` baseline, sanitizes already-managed CodexNeo/legacy API overrides before saving that baseline, restores the baseline on Revert when present, preserves plain user `model_provider = "openai"` when no managed `openai_base_url` exists, and prunes only web-created `config.toml.codexneo-backup-*` files opportunistically by age/count while never pruning the protected baseline or unrelated Windows app backups.
- RED CN-029/CN-030 focused: backend tests initially failed because no original baseline was created, Revert removed plain `model_provider = "openai"`, and old CodexNeo backups were not pruned; frontend test initially failed because `Unselect all` never appeared.
- GREEN CN-029/CN-030 focused: Auth->API baseline/sanitization/plain-provider/retention tests pass (`4 passed`); Select all toggle component test passes.
- GREEN CN-029/CN-030 broad verification before live reload: CodexNeo/Accounts backend regression suite passes (`90 passed`); CodexNeo frontend suite passes (`22 passed`); Ruff passes; OpenSpec strict validation passes; TypeScript passes; Vite production build passes with the existing large-chunk warning; `git diff --check` exits 0 with only existing LF-to-CRLF warnings. Codex Desktop was not restarted during verification.
- GREEN CN-029/CN-030 live reload/smoke: restarted only native codex-lb, not Codex Desktop. Old Python listener PID 66512 was stopped; final listener PID is 28416 and `/health` returned `ok`. Live `/api/codexneo/accounts` returned 12 rows. Headless Chromium smoke on `http://127.0.0.1:2455/codexneo` selected all 12 rows, then Unselect all cleared back to 0 selected rows, with 0 console warnings/errors.
- 2026-06-26: Completed CN-031 after user requested sortable account headers and Activity log retention capped to the latest 1000 entries. Added a source-order `#` column and sortable headers for `#`, Plan, 5h, Weekly, Avail, and Status / Last. Sorting operates on a derived row list, preserves selected account keys, and does not mutate Codex/Backup location state. Activity log append now prunes the on-disk temporary log to the latest 1000 lines.
- RED CN-031 focused: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_activity_log.py::test_activity_log_retains_only_latest_1000_lines -q` initially failed with 1005 retained lines; `bun run test src/features/codexneo/components/codexneo-page.test.tsx -t "sorts account rows"` initially failed because the sortable `#` column was missing.
- GREEN CN-031 verification before live reload: activity log tests pass (`2 passed`); CodexNeo page tests pass (`18 passed`); broad CodexNeo/Accounts backend suite passes (`91 passed`); CodexNeo/header frontend suite passes (`19 passed`); Ruff passes; TypeScript passes; OpenSpec strict validation passes; Vite production build passes with the existing large-chunk warning; `git diff --check` exits 0 with only existing LF-to-CRLF warnings. Codex Desktop was not restarted during verification.
- GREEN CN-031 live reload/smoke: restarted only native codex-lb, not Codex Desktop. Old Python listener PID 28416 was stopped; final listener PID is 13464 and `/health` returned `ok`. Headless Chromium smoke on `http://127.0.0.1:2455/codexneo` found 12 rows, confirmed `#` sort asc first row `1`, `#` sort desc first row `12`, confirmed Plan/5h/Weekly/Avail/Status sortable header states, and saw 0 console warnings/errors.
- 2026-06-26: Completed CN-032 through CN-035. CN-032 hides visible CodexNeo sortable-header helper labels (`SORT`/`ASC`/`DESC`) while preserving sorting and removes the selected-account validity date controls. CN-033 updates Settings API-key edit selectors so exact typed account/model searches commit on close or Enter and enforced model changes are submitted. CN-034 adds `scripts/import_codexneo_windows_usage.py`, an idempotent dashboard-only rough usage import for CodexNeo Windows aggregate values (337,735,265 tokens today, about 2.5B lifetime tokens, about $8342.81 estimated cost). CN-035 adds hidden Windows startup launchers under `scripts/windows/` and registers `Codex IB Startup.vbs` in the current user's Startup folder. The native codex-lb server was started/reloaded; Codex Desktop was not restarted.
- GREEN CN-032/CN-033 frontend: `node node_modules\vitest\vitest.mjs run src\features\codexneo\components\codexneo-page.test.tsx src\features\api-keys\components\api-key-edit-dialog.test.tsx` passes: 2 files, 36 tests.
- GREEN CN-034 backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_import_codexneo_windows_usage.py -q` passes; `.venv\Scripts\python.exe -m ruff check scripts\import_codexneo_windows_usage.py tests\unit\test_import_codexneo_windows_usage.py` passes; actual import run is idempotent and writes `codexneo-windows-import-today` plus `codexneo-windows-import-prior`.
- GREEN CN-032 through CN-035 quality/live: `openspec validate add-codexneo-windows-integration --strict` passes; TypeScript `tsc -b` passes; Vite production build passes with the existing large-chunk warning; `git diff --check` exits 0 with only existing LF-to-CRLF warnings. Live `/health` returns `ok`, `/codexneo` returns HTTP 200, 1-day dashboard shows 337,735,265 tokens and $1127.064458 from the imported today row, and 7-day dashboard shows 2,500,413,327 tokens and $8344.229459 including existing local logs plus the imported lifetime row. Rendered Playwright smoke found 12 CodexNeo rows, no visible `SORT`/`ASC`/`DESC`, no `Validity date`, and 0 console warnings/errors.
- Current status: CodexNeo backend/frontend implementation, tests, OpenSpec change folder, static frontend assets, Windows startup launcher files, and root handover are updated in the working tree.

## Remaining Work

- CN-018 is verified: compact CodexNeo health badges are implemented for Codex IB server/API, Codex Home, registry/snapshot count, app-owned Backup store, Accounts sync, activity-log streams, CodexGO auth configuration, and OpenAI-compatible bridge reachability with safe copy actions.
- CN-019 is verified: Codex/Backup all parity behavior now matches the Windows visible-table semantics, with discovery backup gated on Backup-all.
- CN-020 is verified: CodexNeo visible rows dedupe by real auth identity across Codex Home/Backup snapshots; generated Accounts `__copy` rows are hidden only when upstream identity and plan slot match, without deleting historical database rows.
- CN-021 is verified: CodexNeo status fallback now uses explicit registry status first, then timeout override, usage-window freshness for Codex-present rows, backup-only stored status, and `Unknown` only as last resort.
- CN-022 is verified: Select all selects all currently visible CodexNeo account rows without changing Codex/Backup location checkboxes.
- CN-023 is verified: CodexGO interval label now shows `Every (min)`.
- CN-024 is verified: Codex Home Accounts Refresh shows fetch/completion feedback and was rendered-smoked against the live server.
- CN-025 is verified: follow-up audit fixes are implemented, while Auth->API Set/Revert restart behavior remains intentionally unchanged for runtime use.
- CN-026 is verified: multi-selected Delete selected and Accounts-tab Codex Home delete call `codex-auth remove` one selector at a time; CodexNeo now has an explicit `Sync` action and API that reconciles Codex Home/Backup snapshots with Codex IB and writes a token-free master registry.
- CN-027 is verified: Accounts-page delete and CodexNeo Delete selected pre-detect unsupported configured Codex registry schema versions, bypass incompatible `codex-auth remove`, and fall back to local Codex Home/Backup registry deletion for equivalent runtime schema errors; unrelated remove failures remain safe failures.
- CN-028 is verified: Backup-only CodexNeo delete now handles encoded Windows snapshot filenames for unsafe account keys, and Delete selected API failures show a visible error toast instead of appearing silent.
- CN-029 is verified: Select all now toggles to Unselect all when every visible account row is selected, then clears only row selections.
- CN-030 is verified: Auth->API config Set/Revert now protects a true original config baseline, avoids treating already-managed CodexNeo API configs as original, and prunes old web-created config backups without deleting the baseline or unrelated Windows app backups.
- CN-031 is verified: CodexNeo accounts table has a source-order `#` column plus sortable Plan, 5h, Weekly, Avail, and Status / Last headers, and the Activity log file retains only the latest 1000 entries.
- CN-039 is verified: CodexNeo read routes are display-only, explicit/auto sync remains on the write-protected sync route, generated `__copy` cleanup is confined to write paths, direct Activity log appends respect stream settings, location payloads are strict, and backend/frontend quality gates are clean.
- Future requested features should be added to `CODEXNEO_IMPLEMENTATION_LIST.md` until the user asks to implement them.
- CN-008 is verified: CodexNeo import/delete and Accounts tab import/delete now sync across Codex Home and the encrypted Accounts database with safe failure reporting and cross-tab invalidation.
- CN-009 is verified: Activity log content is fixed-height/scrollable, and coverage remains bounded to configured `/v1/*` and `/api/*` streams with `/api/codexneo/activity-log` skipped.
- CN-010 is verified and superseded by CN-014 where noted: the CodexNeo account table no longer shows `API`, `API h`, `API d`, or row-level `Use API`; row Switch/Switch & Restart remain; `Avail` has been restored by CN-014.
- CN-011 is verified for import/export picker fallback, app-owned backup store, Codex/Backup row checkboxes, safety blocking, and delete-everywhere behavior. CN-019 supersedes the original sticky Codex all/Backup all interpretation with Windows visible-table parity.
- CN-012 is verified: blank Import file/folder actions now use browser upload inputs and backend upload routes, while blank backend path imports return safe non-blocking failures instead of launching server-side Windows pickers.
- CN-013 is verified: Export all no longer depends on the missing `codex-auth export` command and now exports known Codex Home/Backup snapshots natively; the Windows `codex-auth.cmd` shim is resolved for remaining command-backed actions.
- CN-014 is verified: selected-account Use API is removed end to end, refresh replaces it, `Avail` is restored, and `Status / Last` is formatted from usage status plus relative last activity.
- CN-015 is verified: CodexNeo refresh and CodexGO auth writes now sync valid Codex Home/Backup auth snapshots into the encrypted Accounts database without adding a Codex login/device-login flow in Codex IB.
- CN-016 is verified: CodexGO Use/Refresh now invalidates CodexNeo and main Accounts queries, automatic discovery sync is idempotent for existing upstream identities, the sync message says `unique account snapshot(s)`, and the selected-account toolbar/table row layout is stable. Existing duplicate `__copy` Accounts rows were not removed because that is a data cleanup decision requiring explicit permission.
- 2026-06-26: Completed CN-036 after user asked to keep Refresh selected multi-select and fix the root cause of CodexNeo/Accounts/Dashboard mismatches from stale duplicate Backup copies. Root cause: Accounts-tab delete cleaned live Codex Home through `codex-auth remove` but did not delete matching app-owned Backup snapshots on successful CLI removal, and Backup writes upserted only by account key, allowing the same real auth identity to accumulate under older backup keys. Fixed equivalent cleanup across live registry rows, backup registry rows, and snapshot-only files by auth claims; Accounts-tab delete now removes matching Backup-only data too; Backup creation now replaces older app-owned Backup copies for the same auth identity. Refresh selected already sent all selected account keys and was left unchanged.
- RED CN-036 focused: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_locations.py::test_backup_present_replaces_older_backup_copy_for_same_auth_identity tests\unit\test_codexneo_account_discovery_sync.py::test_accounts_delete_removes_matching_backup_when_codex_auth_remove_succeeds tests\unit\test_codexneo_account_discovery_sync.py::test_accounts_delete_removes_backup_only_match_when_codex_home_has_no_live_key -q` initially failed on duplicate Backup registry rows, leftover Backup snapshot, and backup-only delete returning count 0.
- GREEN CN-036 focused: the same focused test command passes: 3 passed.
- GREEN CN-036 backend slice: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_account_discovery_sync.py tests\unit\test_codexneo_import_export_actions.py tests\integration\test_accounts_api.py tests\integration\test_codexneo_account_discovery_sync_api.py tests\integration\test_codexneo_accounts_sync_api.py -q` passes: 51 passed.
- GREEN CN-036 quality/live: `.venv\Scripts\python.exe -m ruff check app\modules\codexneo\locations.py app\modules\codexneo\sync.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_account_discovery_sync.py` passes; `npx --yes openspec validate add-codexneo-windows-integration --strict` passes; `git diff --check` exits 0 with only existing LF-to-CRLF warnings. Restarted only native codex-lb, not Codex Desktop; final listener PID is 68776 and `/health` returned `ok`. Read-only live smoke: `/api/codexneo/accounts` returns 14 rows with 0 duplicate email groups, `/api/codexneo/health` reports `14 CodexNeo account(s), 14 Codex IB account(s)` and `14 backup account(s), 14 auth snapshot(s)`, and `/codexneo` returns HTTP 200.
- 2026-06-26: Completed CN-037 and CN-038 after user approved implementing all pending list items. CN-037 adds page-scoped Codex Home auto refresh controls (`Auto refresh Codex Home`, `Every (sec)`, default 30, clamped 5-3600) plus `Auto sync Codex IB`; auto refresh refetches CodexNeo accounts and diagnostics, and auto sync runs the existing master sync when the Accounts sync diagnostic reports a mismatch. CN-038 removes CodexNeo browser confirmation dialogs from Restart Codex, Switch, Switch & Restart, and Delete selected, adds switch error toast handling, and adds a local schema-compatibility fallback for Switch/Switch & Restart that sets the active Codex Home account and root `auth.json` when installed `codex-auth` cannot read newer registry schema versions. Switch & Restart still performs the intended runtime Codex Desktop restart after a successful switch; verification did not restart Codex Desktop.
- RED CN-037/CN-038 focused: new CodexNeo page tests initially failed because the auto refresh/sync controls did not exist and confirmation calls were expected; new backend switch fallback tests initially failed because schema 5 switch still depended on the external command path. A live smoke also exposed that Accounts sync considered `19 CodexNeo account(s), 20 Codex IB account(s)` aligned; fixed so any unequal count is red/error `Mismatch`.
- GREEN CN-037/CN-038 verification: `.venv\Scripts\python.exe -m pytest tests/unit/test_codexneo_health.py tests/unit/test_codexneo_import_export_actions.py -q` passes: 19 passed. Broad CodexNeo/Accounts backend slice passes: 97 passed. `npx vitest run src/features/codexneo` passes: 2 files, 28 tests. `.venv\Scripts\python.exe -m ruff check app tests scripts` passes. `npx tsc -b` passes. `npx vite build` passes with the existing large-chunk warning. `npx openspec validate add-codexneo-windows-integration --strict` passes.
- GREEN CN-037/CN-038 live reload/smoke: restarted only native codex-lb, not Codex Desktop. Old uvicorn listener PID 41444 was stopped; final listener is `python.exe` PID 41876 running `.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 2455`, and `/health` returned `ok`. Final read-only live smoke: `/api/codexneo/health` reports `overallStatus=ok`, `Accounts sync=ok`, `20 CodexNeo account(s), 20 Codex IB account(s)`; `/api/codexneo/accounts` returns 20 rows; `/codexneo` returns HTTP 200. Headless Playwright DOM smoke found `CodexNeo`, `Auto refresh Codex Home`, `Codex Home refresh seconds` value `30`, `Auto sync Codex IB`, `Sync`, and 0 console errors.
- 2026-06-26: Completed CN-039 review cleanup after user approved preserving auto sync, deleting generated `__copy` rows via write cleanup, and keeping runtime Auth->API Set/Revert restarts intentional. Fixed `GET /api/codexneo/accounts` so it is read-only and no longer syncs/imports Accounts rows; explicit/auto sync remains on `POST /api/codexneo/accounts/sync`. Moved generated-copy cleanup off read-only Accounts listing and kept consolidation on import/sync write paths. Added strict `codex` / `backup` location request types, gated direct Activity log appends by saved stream settings, fixed backend `ty` diagnostics, and removed CodexNeo page hook lint blockers by deriving settings/Codex Home values from query data plus local user overrides.
- RED CN-039 focused: `pytest tests\integration\test_codexneo_account_discovery_sync_api.py -q` initially failed because GET still called sync and invalid `location=archive` returned 200. `pytest tests\unit\test_codexneo_activity_log.py -q` initially failed because `CodexNeoActivityLogService` had no settings gate. `ty check app` initially reported 4 diagnostics for broad location types and import path narrowing.
- GREEN CN-039 backend: `.venv\Scripts\python.exe -m pytest tests\unit\test_codexneo_account_discovery_sync.py tests\unit\test_codexneo_accounts.py tests\unit\test_codexneo_activity_log.py tests\unit\test_codexneo_command_runner.py tests\unit\test_codexneo_health.py tests\unit\test_codexneo_home_ops.py tests\unit\test_codexneo_import_export_actions.py tests\unit\test_codexneo_locations.py tests\unit\test_codexneo_restart.py tests\unit\test_codexneo_scheduler.py tests\unit\test_codexneo_service.py tests\integration\test_codexneo_account_discovery_sync_api.py tests\integration\test_codexneo_accounts_sync_api.py tests\integration\test_codexneo_health_api.py tests\integration\test_codexneo_upload_import_api.py tests\integration\test_accounts_api.py::test_list_accounts_hides_generated_copy_rows_for_same_identity tests\integration\test_accounts_repository.py::test_consolidate_generated_copy_duplicates_keeps_active_copy_and_usage -q` passes: 80 passed.
- GREEN CN-039 quality/frontend: `.venv\Scripts\python.exe -m ruff check app/modules/codexneo app/modules/accounts/service.py tests/integration/test_codexneo_account_discovery_sync_api.py tests/unit/test_codexneo_activity_log.py tests/integration/test_accounts_api.py` passes; `.venv\Scripts\python.exe -m ty check app` passes; `& "$env:USERPROFILE\AppData\Local\Microsoft\WinGet\Links\bun.exe" run lint -- src/features/codexneo/components/codexneo-page.tsx` passes; `bun run test -- src/features/codexneo` passes: 2 files, 28 tests; `bun run typecheck` passes; `bun run build` passes with only the existing large-chunk warning; `npx --yes openspec validate add-codexneo-windows-integration --strict` passes.

- 2026-06-26: Fixed hidden Windows startup BAT after user reported Codex IB did not come up and no port was visible. Root cause from %LOCALAPPDATA%\CodexIB\codex-ib-startup.log: astapi.exe/Rich crashed on a Unicode rocket under Windows cp1252. Launcher now logs the URL http://127.0.0.1:2455/codexneo, sets UTF-8 environment variables, and starts via .venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 2455 instead of the Rich FastAPI CLI.

- 2026-06-26: Started CN-040 portable Windows package work. Added scripts/windows/package-codex-ib-portable.ps1 to build dist\CodexIB-Portable with hidden VBS/PowerShell launcher, portable-data, logs, app/config/runtime files, and local .venv copy; launcher opens http://127.0.0.1:2455/codexneo without starting/restarting Codex Desktop.

- 2026-06-26: CN-040 verification found and fixed a portable launcher log wording issue: -NoBrowser already skipped opening the browser, but the existing-server log still said opening browser. Updated the generated launcher template before rebuilding the portable folder.

- 2026-06-26: Completed CN-040 portable Windows package. Built dist\CodexIB-Portable with app/config/static assets, local .venv runtime, CodexIB-Portable.vbs hidden launcher, CodexIB-Portable.ps1 launcher, README-PORTABLE.txt, logs, and portable-data. Verification: package script parsed; package build ran successfully; generated launcher parsed; no-browser launcher smoke detected existing healthy http://127.0.0.1:2455/health and did not open a browser; /health and /codexneo returned HTTP 200; package size is about 155.2 MB; git diff --check exited 0 with only existing LF-to-CRLF warnings. Codex Desktop was not restarted.

- 2026-06-26: CN-040 full portable runtime smoke completed. Temporarily stopped the native source server, launched dist\CodexIB-Portable\CodexIB-Portable.ps1 -NoBrowser, verified the portable server command came from dist\CodexIB-Portable and returned HTTP 200 for /health and /codexneo, then stopped the portable server and restored the normal source checkout server on port 2455 from .venv\Scripts\python.exe. Codex Desktop was not restarted.

- 2026-06-26: Started CN-041 Electron portable package. Added desktop/electron/main.cjs, desktop/electron/package.json, and scripts/windows/package-codex-ib-electron-portable.ps1. Design: folder-portable Electron EXE plus sidecar app/config/static assets, relocatable uv-managed .venv, portable-data, and logs; target PC should not need Docker, Python, uv, Bun, or Node when the full folder is copied.

- 2026-06-26: Completed CN-041 Electron portable package. Built dist\CodexIB-Electron-Portable with Codex IB.exe, Electron runtime files, sidecar .python CPython 3.13.13 runtime, package-local .venv site-packages, app/config/static assets, portable-data, logs, and README-PORTABLE-ELECTRON.txt. Verification found uv relocatable venv alone still used the user-profile uv Python, so desktop/electron/main.cjs now launches .python\python.exe with .venv\Lib\site-packages on PYTHONPATH and the package script copies the uv-managed Python runtime into .python. Direct backend import uses dist\CodexIB-Electron-Portable\.python\python.exe. Full runtime smoke stopped the source server, launched Codex IB.exe, confirmed backend executable path was dist\CodexIB-Electron-Portable\.python\python.exe, /health=200, /codexneo=200, then restored the source server. Codex Desktop was not restarted.

- 2026-06-27: Removed superseded CN-040 browser/VBS portable package after Electron portable CN-041 became the active distribution path. Deleted dist\CodexIB-Portable and scripts\windows\package-codex-ib-portable.ps1 after path-safety checks. Updated CODEXNEO_IMPLEMENTATION_LIST.md to mark CN-040 Superseded and point portable distribution to CN-041. Verified dist\CodexIB-Electron-Portable still exists. Codex Desktop was not restarted.

- 2026-06-27: Cleaned and preserved the CodexNeo/Electron portable work on dedicated branch codexneo-portable-windows. Commit 87244ef initially pushed to origin/codexneo-portable-windows. Ignored local build artifacts remain local only: dist\ and desktop\electron\node_modules\ are not tracked. Use this branch as the portable customization branch while main can be synced from the original project, then port/cherry-pick portable changes forward as needed.

- 2026-06-27: Started branding update from the user-provided desktop PNG. Generated trimmed black-themed logo/icon assets under `frontend/public/brand/`, `frontend/public/favicon*.png`, `frontend/public/favicon.ico`, and `desktop/electron/assets/`; removed the stale SVG favicon so the old icon is no longer shipped from public assets. Replaced the shared `CodexLogo` component with the generated image asset, updated browser favicon links, changed the CodexNeo page title icon to the same logo, and wired Electron packaging to use `desktop/electron/assets/icon.ico`.

- GREEN branding update verification: TypeScript `bun run typecheck` passes; focused frontend tests pass (`24 passed`); `git diff --check` passes with only existing LF-to-CRLF warnings; production frontend build passed during Electron packaging with the existing large-chunk warning; Electron portable rebuild completed and `dist\CodexIB-Electron-Portable\app\static\brand\` plus favicon assets contain the generated images; `openspec validate add-codexneo-windows-integration --strict` passes. Restarted only native codex-lb, not Codex Desktop; `/health` returned `ok`. Rendered Playwright smoke on `http://127.0.0.1:2455/codexneo` found two loaded `/brand/codex-ib-logo.png` instances, updated favicon links, and 0 console warnings/errors.

- 2026-06-27: Completed CodexNeo preference persistence and Electron minimize-to-tray controls after user reported Auto refresh Codex Home / Every (sec) / Auto sync Codex IB did not survive closing Codex LB. Root cause: those controls were React-only state and were absent from the CodexNeo settings schema/file. Added durable settings fields for Codex Home auto refresh, refresh seconds, auto sync, and minimize-to-tray; frontend now initializes from settings and auto-saves those controls as they change. Added Electron `preload.cjs` bridge plus tray handling in `desktop/electron/main.cjs`: when Minimize to tray is enabled, the Minimize button hides the Electron window to the tray, and window close also hides to tray until Quit is selected from the tray menu; when disabled, Minimize uses normal window minimize behavior and any existing tray icon is removed.
- GREEN preference/tray verification: RED/GREEN backend preference persistence test passes; RED/GREEN frontend persistence/minimize bridge tests pass; CodexNeo backend slice passes (`20 passed`); CodexNeo frontend suite passes (`30 passed`); frontend lint passes for touched files; TypeScript passes; Ruff passes; `ty check app` passes; production build passes with existing large-chunk warning; OpenSpec strict validation passes; `git diff --check` passes with only LF-to-CRLF warnings. Restarted only native codex-lb, not Codex Desktop; `/health` returned `ok`. Live API save/get/restore proved the four settings persist in `/api/codexneo`. Rendered Playwright smoke changed the controls through the page, reloaded `/codexneo`, confirmed Auto refresh=true, seconds=45, Auto sync=true, Minimize to tray=true, Minimize disabled in normal browser, 0 console warnings/errors, then restored the original settings. Electron portable package rebuilt; `dist\CodexIB-Electron-Portable\resources\app.asar` contains `main.cjs` and `preload.cjs`.

- 2026-06-27: Completed CN-044 Electron portable `Start with Windows` toggle after user requested an in-app tick button for Windows login startup. Added durable `start_with_windows_enabled` / `startWithWindowsEnabled` settings in the CodexNeo backend and frontend schema, added the CodexNeo `Start with Windows` switch beside the portable tray controls, and added an Electron preload/main bridge that calls `app.setLoginItemSettings({ openAtLogin, path: app.getPath("exe"), args: [] })` for the current packaged `Codex IB.exe`. This is separate from the older native source-checkout Startup VBS launcher; it targets the Electron portable app. The running portable app was not restarted.
- GREEN CN-044 verification: RED/GREEN backend settings persistence test passes; RED/GREEN frontend bridge test passes; RED/GREEN Electron helper test passes; `tests/unit/test_codexneo_service.py tests/unit/test_codexneo_restart.py` passes (`17 passed`); CodexNeo page Vitest suite passes (`26 passed`); `ruff check app/modules/codexneo tests/unit/test_codexneo_service.py` passes; scoped `ty check app/modules/codexneo` passes; frontend `bun run typecheck` passes; frontend lint for touched files passes; production `bun run build` passes with the existing large-chunk warning; OpenSpec strict validation passes. Broad `ty check app` currently fails in unrelated `app/modules/usage` typing around nullable `account_id`, so use the scoped CodexNeo `ty` result for this change unless fixing that pre-existing usage-module issue. Runtime mirror step updated the active portable folder without restarting the app: backend CodexNeo files were copied under `dist\CodexIB-Electron-Portable\app\modules\codexneo`, built static assets were copied under `dist\CodexIB-Electron-Portable\app\static`, and `resources\app.asar` was replaced with a rebuilt Electron shell containing `main.cjs`, `preload.cjs`, and `startup.cjs`.

- 2026-06-27: Completed CN-045 CodexNeo Windows usage parity after user reported the portable CodexNeo usage stats were wrong and Windows app dashboard values were not reflected in the portable app. Active development branch remains `codexneo-portable-windows`; keep using this branch for the portable Windows app and keep `main` as the original/upstream branch. Root cause 1: the Windows CodexNeo dashboard gets aggregate usage from `H:\Opencode IDE\RC Codex Auth Switcher\dist\rc-codex-auth-data\api-routing.json`, using UTC `DayKey` reset logic, while the earlier Codex IB import script used stale hard-coded rough values. Root cause 2: the portable CodexNeo account table read registry `used_percent` values from `%USERPROFILE%\.codex\accounts\registry.json` and displayed them directly, but the Windows CodexNeo table displays remaining usage (`100 - used_percent`) for `5h` and `Weekly`.
- CN-045 implementation details: `scripts\import_codexneo_windows_usage.py` now accepts `--api-routing-json`, reads PascalCase Windows `api-routing.json`, applies the Windows app current-day reset behavior, computes the same GPT-5.5-style lifetime estimate from input/output/cached lifetime counters, and deletes both new `codexneo-windows-import-*` rows and older `codexneo-windows-estimate-*` rows before inserting replacement synthetic dashboard rows. Current imported Windows aggregate from the live file was `tokensToday=0`, `tokensLifetime=2,811,043,428`, `cachedInputLifetime=1,095,390,208`, and estimated cost `$9380.184579`. The active portable DB was backed up first at `dist\CodexIB-Electron-Portable\portable-data\store.db.bak-cn045-usage-20260627-212329`, then imported into `dist\CodexIB-Electron-Portable\portable-data\store.db` with `CODEX_LB_DATA_DIR` set to that portable data directory. Legacy synthetic rows were removed; current synthetic rows are `codexneo-windows-import-today` and `codexneo-windows-import-prior`.
- CN-045 account table parity details: `app\modules\codexneo\schemas.py` adds `remaining_percent`; `app\modules\codexneo\accounts.py` derives and clamps it from registry usage windows; `frontend\src\features\codexneo\schemas.ts` accepts `remainingPercent`; and `frontend\src\features\codexneo\components\codexneo-page.tsx` displays/sorts `5h` and `Weekly` by `remainingPercent`, falling back to `100 - usedPercent` for older responses. Example: registry `used_percent` values `1` and `91` now render as `99%` and `9%`, matching the Windows app instead of the old portable display of `1%` and `91%`.
- CN-045 verification: RED tests failed before implementation for missing `remaining_percent` and frontend still rendering/sorting by `usedPercent`. GREEN focused verification passed: `uv run pytest tests/unit/test_codexneo_accounts.py::test_registry_account_discovery_returns_safe_fields_and_active_marker tests/unit/test_codexneo_accounts.py::test_registry_usage_windows_expose_remaining_percent_for_windows_parity tests/unit/test_import_codexneo_windows_usage.py -q` (`4 passed`); `npx vitest run src/features/codexneo/components/codexneo-page.test.tsx -t "renders the Codex home accounts table|sorts account rows"` (`2 passed`). Broader verification passed: `uv run pytest tests/unit/test_codexneo_accounts.py tests/unit/test_import_codexneo_windows_usage.py -q` (`10 passed`); full CodexNeo page Vitest suite (`26 passed`); `uv run ruff check app/modules/codexneo/accounts.py app/modules/codexneo/schemas.py scripts/import_codexneo_windows_usage.py tests/unit/test_codexneo_accounts.py tests/unit/test_import_codexneo_windows_usage.py`; frontend `bun run typecheck`; frontend `bun run build` with only the existing large-chunk warning. Live dashboard smoke after the import returned HTTP 200 for `/api/dashboard/overview?timeframe=7d`; the dashboard total is higher than the Windows screenshot because it includes the imported Windows aggregate plus other real Codex IB request logs already in the portable DB.
- CN-045 portable runtime note: The running portable Electron app was not restarted. Source code and built static assets were updated, then `app\modules\codexneo\accounts.py`, `app\modules\codexneo\schemas.py`, and built `app\static` assets were mirrored into `dist\CodexIB-Electron-Portable` for the user's next manual restart. Portable backend syntax check with `dist\CodexIB-Electron-Portable\.python\python.exe -m py_compile` passed. Do not restart the portable app or Codex Desktop unless the user explicitly asks.

- 2026-06-28: Completed CodexNeo Home Accounts toolbar cleanup on the active `codexneo-portable-windows` branch. The portable branch remains the only branch for CodexNeo Windows portable development; keep `main` as the original/upstream branch and port only needed upstream changes into this side branch. The selected-account bulk toolbar no longer exposes `Temp unavailable`, `Mark available`, `Switch`, or `Switch & Restart`; row-level `Switch` and `Switch & Restart` remain available in the account table. The control area is organized around the requested operational controls: Codex Home status, Auto refresh Codex Home, Every (sec), Auto sync Codex IB, Refresh, Sync, Minimize to tray, Start with Windows, and Minimize.
- 2026-06-28: Fixed the real app-logic cause of the CodexNeo/Codex IB false account-count mismatch after the user clarified the extra `CodexNeo.txt` source was manually corrupted and should not be treated as the root issue. Root cause: `/api/codexneo/health` counted raw CodexNeo account sources from `CodexNeoAccountLocationService.all_account_rows()`, while the visible CodexNeo account table dedupes equivalent root `auth.json`, live registry rows, and app-owned Backup snapshots by real auth identity. This allowed a root `C:\Users\rcgok\.codex\auth.json` plus a Backup snapshot for the same account to report a false `19 CodexNeo / 18 Codex IB` mismatch even though the table correctly showed one identity.
- CN-046/CN-047 implementation details: `app\modules\codexneo\health.py` now uses the same `_dedupe_registry_items()` identity merge as the account table and reads `active_account_key` safely from `%USERPROFILE%\.codex\accounts\registry.json` only to preserve active-row preference. `tests\unit\test_codexneo_health.py` includes a regression where root `auth.json` and Backup contain the same auth identity and the health badge must report `1 CodexNeo account(s), 1 Codex IB account(s)`. OpenSpec task/spec entries were updated for the toolbar order and the health-count identity dedupe behavior.
- CN-046/CN-047 verification and runtime note: RED/GREEN health regression was verified; focused health suite passed (`5 passed`); related CodexNeo account/location/discovery backend slice passed (`35 passed`); Ruff passed for `app\modules\codexneo\health.py` and `tests\unit\test_codexneo_health.py`; `openspec validate --specs` passed (`30 passed`); `git diff --check` exited 0 with only existing LF-to-CRLF warnings. The fixed backend module was mirrored to `dist\CodexIB-Electron-Portable\app\modules\codexneo\health.py` and hash-checked against the source file. Only the portable Python backend on port `2455` was restarted so it would load the patched module; the Electron portable UI and Codex Desktop were not restarted. Live `/api/codexneo/health` reported `Accounts sync` = `ok`, `Counts aligned`, `19 CodexNeo account(s), 19 Codex IB account(s)`.

- 2026-06-28: Completed CN-048 Electron single-instance and fast CodexNeo account selection. `desktop\electron\single-instance.cjs` now wraps Electron `app.requestSingleInstanceLock()` so a second `Codex IB.exe` launch exits before booting another backend/window and sends focus to the existing window through the `second-instance` event. `desktop\electron\main.cjs` claims that lock before `app.whenReady().then(boot)`. CodexNeo account selection now uses `frontend\src\features\codexneo\components\account-selection.ts`; range selection applies over the current sorted visible row order, with the prior clicked row or current visible selection as anchor. Shift-click range selection changes only selected account keys and does not mutate Codex/Backup location checkboxes.
- 2026-06-28: Completed CN-049 GPT-5.5 estimate pricing source check. Current standard GPT-5.5 pricing was verified from provider pricing references as input `$5.00/1M`, cached input `$0.50/1M`, and output `$30.00/1M`. `app\core\usage\pricing.py` already had those standard rates. The real hardening change is in `scripts\import_codexneo_windows_usage.py`: the CodexNeo Windows import now resolves `gpt-5.5` through shared `app.core.usage.pricing` and `calculate_cost_from_usage()` instead of carrying duplicated cost math. This keeps future imports aligned with the same pricing path used by request-log/dashboard cost calculation. Note: long-context and priority GPT-5.5 tiers are higher; the Windows aggregate import still uses the standard estimate because `api-routing.json` aggregate counters do not include per-request service-tier data.
- CN-048/CN-049 verification and runtime note: RED/GREEN focused checks passed for Electron single instance (`node --test desktop\electron\startup.test.cjs desktop\electron\single-instance.test.cjs`, 4 tests), range selection helper (`account-selection.test.ts`, 2 tests), CodexNeo page suite (`30 tests` with the helper and Shift-click component regression included), and pricing/import backend (`3 passed` for `test_import_codexneo_windows_usage.py`). A post-resume rendered smoke found that the range anchor was being read inside React's scheduled state updater after the ref had already advanced to the clicked row; `codexneo-page.tsx` now captures the anchor and Shift state before scheduling the update. Final verification included Ruff for touched import tests/script, frontend typecheck, production frontend build, OpenSpec strict validation, `git diff --check` with only existing LF-to-CRLF warnings, standalone Playwright held-Shift smoke against `http://127.0.0.1:2455/codexneo` showing the first three row checkboxes selected, and no console issues. Built `app\static` was mirrored to `dist\CodexIB-Electron-Portable\app\static`, and `dist\CodexIB-Electron-Portable\resources\app.asar` contains `main.cjs`, `preload.cjs`, `startup.cjs`, and `single-instance.cjs` with the single-instance boot guard. The running Electron portable app was not restarted, so the single-instance behavior applies after the user's next manual portable app restart.

- 2026-06-30: Completed CN-050 CodexNeo/Codex IB usage-status reconciliation after the user reported that freshly synced Codex IB accounts in CodexNeo showed `-` usage at first, and later paused/reauth accounts still appeared as `Ready` / `Fresh` because CodexNeo only trusted Codex Home registry freshness. Root cause: CodexNeo read rows had no Codex IB status overlay; when usage was present the mapper formatted the row as `Fresh / ...` regardless of `Account.status`, and CodexNeo could not expose the Accounts-tab auth/routing state.
- CN-050 implementation details: `app\modules\codexneo\accounts.py` now uses a read-only `load_accounts_with_codex_ib_usage()` path that matches each CodexNeo row to exactly one Codex IB account by account id or unique normalized email, fills missing/older primary and secondary usage windows from latest Codex IB `usage_history`, preserves newer registry windows, and refuses ambiguous duplicate-email fallback. The mapper snapshots only primitive Codex IB account state while the SQLAlchemy session is open, exposes `codexIbAccountId`, `codexIbStatus`, `codexIbStatusReason`, and `codexIbRoutable`, and overlays non-active status labels such as `Paused`, `Re-auth required`, `Rate limited`, or `Quota exceeded` onto `Avail` and `Status / Last` without removing visible usage values. Frontend schema accepts the new fields; the existing table renders the corrected backend `availability` and `status` labels.
- CN-050 verification and runtime note: RED/GREEN focused account mapper tests passed, including `test_codex_ib_auth_status_overrides_fresh_usage_label`; focused CodexNeo account/API verification passed (`16 passed`); Ruff passed for touched Python files; frontend `bun run typecheck` passed; `openspec validate add-codexneo-windows-integration --strict` passed; source `accounts.py` and `schemas.py` were mirrored to `dist\CodexIB-Electron-Portable\app\modules\codexneo` with matching SHA-256 hashes; portable backend `py_compile` passed. After the user restarted the portable app, live `/health` returned `ok` and the active listener was `dist\CodexIB-Electron-Portable\.python\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 2455`. Live `/api/codexneo/accounts` showed `r.c.gokulkumar@gmail.com` as `Paused / just now` with `codexIbStatus=paused`, `codexIbRoutable=false`, and 5h/weekly remaining values still present; `t0103.8.232.755@gmail.com` showed `Re-auth required / 1d ago` with the invalidated-token reason and `codexIbRoutable=false`; `zbigzzbigz@gmail.com` was active in current live data and correctly remained available with usage.

- 2026-07-03: Completed CN-051 guarded usage-plan reconciliation and CodexNeo table parity. `app\core\config\settings.py` added `CODEX_LB_USAGE_REFRESH_ALLOW_PLAN_RECONCILE`; `app\modules\usage\updater.py` allows plan reconciliation only when the setting is enabled and no workspace identity conflict is present; `.env.example` documents the switch; the local ignored portable `.env` enables it for the active portable runtime. `app\modules\codexneo\accounts.py` now carries matched Codex IB `plan_type`, monthly usage, effective quota status, and routability into CodexNeo rows so stale registry/Auth plan values no longer drive the table after reconcile.
- CN-051 verification and runtime note: focused and expanded usage/CodexNeo tests passed (`85 passed` in the final combined backend pass reported by the implementation turn); Ruff passed; `openspec validate allow-usage-plan-reconcile --strict` and `openspec validate --specs` passed; source and portable runtime copies compiled; the portable backend was restarted on port `2455`; live `/api/codexneo/accounts` returned the target rows as `plan=free`, `codexIbStatus=quota_exceeded`, and `routable=false`. Commit `be6f334` contains this code/spec/test change.
- 2026-07-03: Completed CN-052 CodexGO Management activity-log visibility. The background CodexGO refresh scheduler now appends token-free Management-stream entries for skipped, started, applied, and failed outcomes through `CodexNeoActivityLogService`, so operators can see scheduler activity in the existing CodexNeo Management log instead of checking `auth.json` rewrite and backup timestamps.
- CN-052 verification and runtime note: `uv run pytest tests/unit/test_codexneo_scheduler.py -q` passed (`3 passed`); Ruff passed for scheduler/test files; `openspec validate add-codexneo-windows-integration --strict` and `openspec validate --specs` passed; the changed scheduler was mirrored into the portable runtime; the portable backend was restarted with the repo `.venv\Scripts\python.exe` interpreter after a failed embedded `.python` launch showed `uvicorn` was unavailable there; a forced `refresh_once=True` pass wrote `Management task CodexGO auto-refresh -> started.` and `Management task CodexGO auto-refresh -> applied; 2301ms.`; `/health` returned `ok`. Commit `7cc90c8` contains this code/spec/test change.
- 2026-07-03: Completed CN-053 CodexNeo auto-delete for previous paid accounts that are now free or auth-required after the user requested a toggle in the Codex Home accounts toolbar. Added a durable `auto_delete_free_reauth_accounts_enabled` / `autoDeleteFreeReauthAccountsEnabled` setting and the UI switch labeled `Auto delete free/auth required` beside the bulk selection controls. When enabled, the page triggers the new write endpoint after account-list refreshes.
- CN-053 implementation details: `POST /api/codexneo/accounts/auto-delete-free-reauth` checks that the persisted toggle is enabled, then deletes only accounts whose Codex Home/root/Backup auth snapshot plan normalizes to `pro` or `plus` and whose matched Codex IB account plan is now `free` or whose matched Codex IB status is exactly `reauth_required`. Matching rows remove the CodexNeo source snapshot through the existing local registry fallback and remove the Accounts row without deleting usage history. `rate_limited`, `paused`, and previously-free accounts are explicitly left untouched by the predicate.
- CN-053 verification and runtime note: RED/GREEN focused backend tests passed for setting persistence and cleanup eligibility; focused frontend tests passed for toggle persistence and auto-run wiring; expanded backend CodexNeo slice passed (`42 passed`); expanded CodexNeo page/hook Vitest suite passed (`35 passed`); Ruff passed for touched backend files; frontend `bun run typecheck` and `bun run build` passed with only the existing large-chunk warning; `openspec validate add-codexneo-windows-integration --strict`, `openspec validate --specs`, and `git diff --check` passed with only existing LF-to-CRLF warnings. The changed backend modules and built static assets were mirrored to `dist\CodexIB-Electron-Portable`; matching hashes were checked for the mirrored backend files, and portable backend `py_compile` passed. The destructive live endpoint was not run against real accounts, and the portable Electron app / Codex Desktop were not restarted.
- CN-053 response-schema hotfix: after the UI showed `Response schema mismatch`, live `GET /api/codexneo` proved the still-running backend omitted `autoDeleteFreeReauthAccountsEnabled` while the new frontend required it. Added frontend schema compatibility so a missing field defaults to disabled, rebuilt/mirrored static assets, restarted only the portable backend on `127.0.0.1:2455` with `CODEX_LB_DATA_DIR=dist\CodexIB-Electron-Portable\portable-data`, and verified live `/api/codexneo` now includes `autoDeleteFreeReauthAccountsEnabled=false`. Rendered Playwright smoke on `/codexneo` showed 0 schema mismatch banners, 0 loading overlays, visible account rows, and no console errors.

- 2026-07-10: Completed CN-057, the first post-GPT-5.6 native API parity pass, without modifying the already-completed Responses Lite `additional_tools` slice. The explicit Restart Codex / Switch & Restart PowerShell now discovers only processes whose executable path belongs to the `OpenAI.Codex` WindowsApps package, requests graceful close from packaged `ChatGPT.exe` shells, force-stops only remaining package processes, and then relaunches the package. It no longer selects a bare `codex.exe` by process name. The generated script parsed successfully in PowerShell, and read-only live discovery matched the current packaged `ChatGPT.exe`, child `codex.exe`, and `codex-code-mode-host.exe` process tree. The restart action itself was not invoked.
- CN-057 model-catalog details: `app\core\clients\model_fetcher.py` no longer removes `model_messages`; it retains the complete upstream model object in `UpstreamModel.raw`. The native `/backend-api/codex/models` serializer already passes unknown JSON-compatible extension fields, so personality messages and future fields now survive upstream fetch -> registry -> native response. Contract-diff coverage verifies both `model_messages` and an unknown nested future field. Bootstrap test expectations now include `gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna` with the runtime's `0.124.0` minimum client version.
- CN-057 Responses details: `V1ResponsesRequest` accepts an omitted `input`/`messages` only when a normalized non-empty `previous_response_id` or `conversation` is present, preserves their mutual exclusion, and converts an input-less continuation to `instructions=""` plus `input=[]`. HTTP `/backend-api/codex/responses` recognizes that OpenAI-compatible shape as well. Standard HTTP Responses routes now return HTTP 400 with `code=unsupported_parameter` and the exact `param` for `background=true`, `store=true`, `max_output_tokens`, `metadata`, `prompt_cache_retention`/`promptCacheRetention`, `safety_identifier`, `temperature`, `top_p`, `truncation`, and `user`. `background=false` and `store=false` remain accepted. This intentionally replaces false-success accept-and-discard behavior on those HTTP routes; background lifecycle retrieval/cancel/delete/input-items remains deferred to the later full-parity pass.
- CN-057 verification: focused unit tests passed (`109` before the final alias/false-flag additions and `104` for the final complete OpenAI request unit file); request-policy/proxy-contract/CodexNeo service tests passed (`76`); model integration passed (`20`); the complete OpenAI compatibility file passed (`67`); neighboring proxy Responses integration passed (`47`); Ruff and scoped `ty` passed; strict OpenSpec change validation and all stable specs passed (`30/30`); `git diff --check` passed with only line-ending warnings. Six changed Python modules were mirrored to both `dist\CodexIB-Electron-Portable\app` and `dist\CodexIB-Electron-Portable\.venv\Lib\site-packages\app`, all source/runtime/site-packages SHA-256 hashes matched, and portable `.python` `py_compile` passed. The running Electron app and its backend were not restarted, so port `2455` continues serving the previous loaded code until the user performs the next portable app restart.
- 2026-07-10: Completed CN-058, the public `/v1` Responses lifecycle parity pass. Added durable API-key-scoped `stored_responses`, `stored_conversations`, and `stored_conversation_items` resources through Alembic revision `20260710_000000_add_responses_lifecycle_resources`. `store=true` now persists synchronous and streamed terminal responses; `background=true` creates a stable local `resp_*` id, runs through the existing routed/accounted Responses execution path, supports polling and active-task cancellation, and preserves the original upstream response id for same-scope continuation. Startup marks stranded queued/in-progress records failed, and graceful shutdown cancels and awaits owned tasks before HTTP/database teardown.
- CN-058 route details: `/v1/responses/{id}` supports retrieve/delete, `/cancel` supports active background cancellation, and `/input_items` returns stable cursor-paginated input records. `/v1/conversations` supports create/retrieve/metadata update/delete, while `/items` supports create/list/retrieve/delete. A Responses call naming a same-scope local conversation expands its existing items before new input without forwarding the local `conv_*` id upstream, then appends successful input/output in order. Local lifecycle records and continuation anchors are hidden across downstream API-key scopes. The native `/backend-api/codex/responses` route keeps CN-057's explicit rejection because it does not own the local public lifecycle.
- CN-058 token-count and limits: `/v1/responses/input_tokens` uses `tiktoken` for deterministic locally visible text/tool JSON and returns `x-codex-lb-token-count: local-compatible`; opaque file/image input and unresolved upstream previous-response context return an explicit 400. These counts are context estimates, not upstream billing usage. `background=true` requires polling with `stream=false`, and stored response retrieval does not replay SSE when `stream=true`. Sampling/output-token/safety/user/prompt-cache-retention controls that the ChatGPT-backed upstream cannot honor still return `unsupported_parameter`; hosted tool behavior remains upstream/model dependent.
- CN-058 verification and runtime note: raw HTTP regressions cover stored synchronous/streaming responses, input items, delete, background completion/cancel/restart recovery, Conversations CRUD/item CRUD/context expansion, deterministic token counts, and opaque-input rejection. Stock OpenAI Python SDK regressions cover store/retrieve/input-items/delete, Conversations/items, input-token count, unknown-id 404, and cross-key isolation including continuation. The final Responses/SDK/proxy gate passed `248` tests; the new Alembic revision passed head/upgrade/downgrade/upgrade verification with only two existing SQLite expression-index reflection warnings. Ruff, scoped `ty`, strict OpenSpec validation, and all `31` stable specs passed after archiving CN-057 and CN-058. Eleven Python files match by SHA-256 across source, portable `app`, and portable site-packages; the portable `.python` + `.venv` runtime imports `tiktoken 0.13.0` and exposes the new routes. Only the portable Codex-LB backend was restarted (new PID `98016`), not Codex Desktop or the Electron shell. Live `/health` returned `ok`, OpenAPI exposed all six checked lifecycle route families, the live database head is `20260710_000000_add_responses_lifecycle_resources` with all three lifecycle tables, live Conversation create/delete returned 200/200 then 404, and live token count returned 200 with `x-codex-lb-token-count=local-compatible`; the smoke conversation was deleted.
- 2026-07-10: Completed CN-059, the scoped GPT-5.6 Standard pricing/cache-write pass. `usage.input_tokens_details.cache_write_tokens` is now typed, clamped non-negative, carried through HTTP stream, WebSocket, compact, proxy/limit/quota warmups, request-log persistence, and API-key reservation settlement. Request logs expose the token count and a separate cache-write cost category; the nullable Alembic column has no invented historical backfill. A review regression also preserves the cache-write cost breakdown when upstream supplies cache-write detail without a cached-read counter.
- CN-059 pricing decisions: the exact generic `gpt-5.6` alias resolves to `gpt-5.6-sol`; Standard short-context cache-write prices are Sol `$6.25/M`, Terra `$3.125/M`, and Luna `$1.25/M`. Long-context, Flex, and API Priority price tables are not part of this pass. Codex subscription Fast and API Priority are distinct: literal tokens remain literal, GPT-5.6 Fast receives one Standard API-equivalent estimate, and upstream credit windows remain authoritative until OpenAI publishes a GPT-5.6 Fast credit multiplier. This avoids multiplying tokens and rates independently.
- CN-059 verification/runtime note: the final backend gate passed `196` tests with only two existing SQLite expression-index reflection warnings; Ruff and scoped `ty` passed. The edited dashboard suites passed `36` tests; frontend TypeScript, ESLint, and the production Vite build passed with only the existing chunk-size warning. Strict change validation and all `31` stable OpenSpecs passed before archive. Eighteen backend/migration files were mirrored to both portable Python locations (`36` SHA-256 matches), all `34` production static files matched, and both portable Python runtimes passed `py_compile`. A repository-wide Vitest run produced no result after five minutes and was terminated; its leftover Vitest processes were stopped. The broader OpenSpec `--all` sweep also retains two unrelated proposal errors: `decompose-proxy-service` has no delta, and `fix-token-invalidated-account-state` has two requirements missing normative MUST/SHALL wording. Codex Desktop, Electron, and the portable backend were not restarted, so port `2455` still serves the previously loaded code and the new migration/code become active on the next portable restart.
- 2026-07-10: Completed CN-060, the GPT-5.6 custom-tool continuation repair for `No tool output found for custom tool call call_*`. The proxy now retains the required output type for ordinary, custom, and apply-patch calls, recognizes custom missing-output errors, and injects only absent typed interrupted outputs on both direct WebSocket and native HTTP-bridge continuations.
- CN-060 recovery detail: the two observed orphan call ids were not available as locally tracked calls, so typed injection alone could not repair them. When the upstream rejects an anchored request before `response.created`, Codex-LB now reconnects and replays once from the safe full transcript already prepared by request normalization, clearing `previous_response_id`. Owner-pinned requests may use this safe repair; short/delta-only requests, second failures, and requests without a transcript that stands independently of the anchor remain fail-closed. This re-sends recorded input; Codex-LB does not execute a tool itself.
- CN-060 verification/runtime note: the exact regression was RED before the repair and GREEN after it. The final focused gate passed `13` tests across direct WebSocket and HTTP bridge, including existing unsafe-continuation and wrong-output-type behavior; Ruff and scoped `ty` passed; strict change validation and all `31` stable OpenSpecs passed. Eight backend modules were mirrored to both portable Python locations (`16` SHA-256 matches), and both portable runtimes passed `py_compile`. Codex Desktop, Electron, and the portable backend were not restarted, so the running backend continues serving its previously loaded code until the next portable restart.

- 2026-07-10: Completed CN-061, the selective reconciliation of merged upstream PRs #1160, #1161, #1163, #1172, #1175, and #1176 against the portable branch's existing CN-057 through CN-060 behavior. Native Codex image generation/edit aliases delegate to the existing image pipeline; a non-empty `/v1/models?client_version=...` returns the same filtered native payload as `/backend-api/codex/models`; and the GPT-5.6 fallback now matches Codex rust-v0.144.1 for the served Sol/Terra/Luna metadata, 372k window, minimum client version, plans, tool modes, Lite/Fast flags, priorities, multi-agent versions, and supported/default reasoning. Sol/Terra expose `ultra`, Luna stops at `max`, and outbound policy maps client/API-key `ultra` to upstream `max` while keeping client-plane configuration intact. Cursor suffix normalization now recognizes all three personality bases without treating `max` or `ultra` as suffix tokens.
- CN-061 Responses detail: the `additional_tools` body item is the authoritative Lite signal. Reserved inbound markers are stripped; canonical HTTP/WebSocket signaling is derived after model/API-key policy; marker-only reuse requires the latest accepted same-model downstream-visible response id; suppressed-created and fresh replay paths keep/reclassify that trust correctly; and bridge trim/owner-forward/retry preserve internally derived state. Typed non-message system/developer directives bypass instruction hoisting and sanitizer removal and remain compact-trim anchors. Function, custom, and apply-patch interrupted outputs are typed and injected before request preparation, so byte guards, fingerprints, stored input, retries, and API-key budgets observe the real forwarded payload. Normal zero-input turns can update continuity; synthetic zero-output prewarm cannot replace the real anchor. When remote-owner forwarding fails and the rebound local session has no pending call state, the anchored request is resubmitted unchanged and any missing-output rejection is masked rather than exposing the raw 400/call id. CN-060's single safe full-transcript retry remains intact.
- CN-061 test-isolation incident and correction: an early broad integration run created `http-bridge-…@example.com` fixture rows because `tests/conftest.py` isolated SQLite/encryption but not the CodexNeo sync service's real Codex Home writes. The run was stopped immediately. A final independent review then caught that `tests/e2e/conftest.py` creates a second application and would have bypassed a root-app-only override. Both the root and e2e application fixtures now share a temporary Codex Home/backup sync service and a disabled native command runner. The formerly leaking integration test and an e2e account-import flow passed while the real account and backup stores remained hash-identical. The user manually deleted the visible fixture accounts. Follow-up checks found zero `http-bridge-…@example.com` or `e2e-…@example.com` rows in live `/api/accounts` and `/api/codexneo/accounts`, and zero matching snapshots in the real Codex account or portable backup stores.
- CN-061 verification/runtime note: backend verification passed `362` grouped core/unit tests, `162` grouped API integration tests, `506` proxy utility tests, `132` complete HTTP/WebSocket bridge tests, and `82` OpenAI compatibility/e2e tests, followed by a guarded isolated e2e account-import rerun. `uv run ruff check app tests` and scoped `ty` across all changed production Python files passed; full `ty check app` still reports five unrelated existing nullable `account_id` diagnostics in unchanged usage mapper/repository code. Frontend verification passed all `106` Vitest files / `682` tests plus typecheck, lint, and production build (only the existing large-chunk warning). Forty-four changed Python sources were mirrored into both portable code roots and all `88` SHA-256 comparisons matched; the exact `34`-file static tree was mirrored into both roots and all `68` comparisons matched; both portable interpreters passed `py_compile`. Strict OpenSpec validation passed all `31` stable specs; `preserve-responses-lite-additional-tools` and `reconcile-upstream-codex-parity-20260710` were archived. The listener remains portable backend PID `98016` on `127.0.0.1:2455`; it, Codex Desktop, and Electron were not restarted to avoid interrupting this active Codex session, so the mirrored code loads on the next safe portable backend restart.
- 2026-07-11: Completed CN-062 for affected Codex task `019f48fe-2256-73f0-8b38-243690307afb`. The repeated compact/sub-agent failure was a local serialization bug, not a GPT-5.6 capability failure: `ResponsesCompactRequest` retained the Lite `additional_tools` envelope while `_strip_compact_unsupported_fields()` removed `parallel_tool_calls`. Core transport therefore added the Responses Lite marker, but upstream received no required false value and returned HTTP 400 `unsupported_value` with `X-OpenAI-Internal-Codex-Responses-Lite requires parallel_tool_calls to be false`. The portable request log contains 12 matching historical GPT-5.6 Sol failures, including the screenshot burst.
- CN-062 wire repair: one final-wire normalizer now forces `parallel_tool_calls=false` whenever the normalized body selects Lite or a trusted linked WebSocket continuation carries the canonical Lite marker. It is applied to direct HTTP and WebSocket mappings, HTTP bridge/owner-forward frames, compact serialization, and both fresh WebSocket retry serializers. The latter was added after independent review proved a safe full-transcript resend could otherwise reconstruct the request from the validated client model and restore `true`. Non-Lite full Responses payloads are unchanged; compact continues removing `tools` and `tool_choice` while explicitly retaining the required false flag.
- CN-062 catalog repair: the shared native builder now emits the upstream-compatible dual shape `{models, object: "list", data}`. Negotiated `/v1/models?client_version=...` remains byte-equivalent to `/backend-api/codex/models`; stable upstream creation metadata, with a deterministic zero fallback, prevents equality from depending on request time. Existing local admission of `supported_in_api=false` compatibility models is preserved, while API-key restrictions and Codex list/hide visibility are applied to `data` so hidden rows do not leak through the OpenAI half.
- CN-062 verification/runtime note: focused tests were RED on the missing false flag and missing catalog fields before implementation. Final gates passed `506` proxy utility tests, `195` OpenAI-request/compact/Responses/catalog tests, and `132` complete HTTP/WebSocket bridge tests. Scoped Ruff and `ty`, strict active-change validation, all `31` stable OpenSpecs, and `git diff --check` passed; the completed change is archived as `2026-07-10-fix-responses-lite-parallel-tools-and-model-catalog-shape`. The first independent review found the fresh WebSocket retry serializer gap; after correction, a second independent review found no P1/P2 issues and passed `29` focused wire/catalog checks. The accumulated frontend files passed `79` focused tests, ESLint, TypeScript, and the production build with only the existing chunk-size warning. The repository-wide Vitest command again produced no result and was terminated after roughly three minutes; its two repo-scoped child processes were also stopped. Forty-five changed Python sources were mirrored to both portable roots with `90` SHA-256 matches; all `34` static files still matched both roots with `68` comparisons. Embedded Python compile passed, and the portable venv imported every CN-062 production module; direct embedded imports remain unavailable because that interpreter intentionally receives dependencies through Electron's `PYTHONPATH`. Live health is still `ok`, both live account APIs and the real `27`/backup `2` JSON stores contain zero fixture markers, and the Lite-error search remains at the same 12 historical rows. The current Electron-owned backend PID `51460` still exposes the pre-fix models-only catalog because it was not restarted. Do not kill and replace that child with a detached backend: `desktop/electron/main.cjs` does not respawn it, and tray exit would no longer own the replacement. A normal full Codex-LB restart loads the mirrored CN-062 code; Codex Desktop itself does not need to be restarted for the proxy update.
